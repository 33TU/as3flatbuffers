// Code generated by as3flatc. DO NOT EDIT.
package fixtures.framing
{
    import as3flatbuffers.Pack;
    import as3flatbuffers.Unpack;
    import as3flatbuffers.UnpackContext;
    import avm2.intrinsics.memory.*;
    import flash.utils.ByteArray;
    import fixtures.framing.Aligned;
    import fixtures.framing.Child;
    import fixtures.framing.Record;

    /** Owned mutable value. pack() writes a complete FlatBuffer into the destination. */
    public final class Record
    {
        private static const PACK:as3flatbuffers.PackContext = new as3flatbuffers.PackContext();
        private static const UNPACK:as3flatbuffers.UnpackContext = new as3flatbuffers.UnpackContext();

        public static const FILE_IDENTIFIER:String = "FRM1";

        /** Check only the four-byte identifier; leaves the input cursor and endian unchanged. */
        public static function hasIdentifier(bytes:flash.utils.ByteArray, offset:uint = 0, sizePrefixed:Boolean = false):Boolean
        {
            const position:Number = Number(offset) + (sizePrefixed ? 8 : 4);
            return bytes != null && position + 4 <= bytes.length &&
                bytes[uint(position)] == 70 && bytes[uint(position) + 1] == 82 &&
                bytes[uint(position) + 2] == 77 && bytes[uint(position) + 3] == 49;
        }

        public var title:String = null;
        public var aligned:fixtures.framing.Aligned = null;
        public var child:fixtures.framing.Child = null;
        public var next:fixtures.framing.Record = null;
        public var values:Vector.<uint> = new Vector.<uint>();

        public static function reset(msg:Record):void
        {
            msg.title = null;
            msg.aligned = null;
            msg.child = null;
            msg.next = null;
            msg.values.length = 0;
        }

        public static function clone(source:Record):Record
        {
            if (!source)
                return null;

            const destination:Record = new Record();
            destination.title = source.title;
            destination.aligned = fixtures.framing.Aligned.clone(source.aligned);
            destination.child = fixtures.framing.Child.clone(source.child);
            destination.next = fixtures.framing.Record.clone(source.next);
            destination.values = source.values.concat();

            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function pack(source:Record, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.beginRoot(context, dst, false, true, 827150918);
                const root:uint = packInto(source, context);
                as3flatbuffers.Pack.finishRoot(context, root, false);
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Write into an active context and return the absolute object offset. */
        public static function packInto(source:Record, context:as3flatbuffers.PackContext):uint
        {
            if (!source || !context)
                throw new ArgumentError("Source and context must be non-null");

            if (source.title == null)
                throw new ArgumentError("Required field title must be set");
            as3flatbuffers.Pack.ensure(context, 93);
            as3flatbuffers.Pack.prepare(context, 2);
            as3flatbuffers.Pack.reserveVtable(context, 5);
            as3flatbuffers.Pack.prepare(context, 16);
            as3flatbuffers.Pack.startTable(context);
            const offset0:uint = as3flatbuffers.Pack.reserveOffset(context, 0);
            if (source.aligned)
                as3flatbuffers.Pack.addStruct(context, 1, fixtures.framing.Aligned.packInto(source.aligned, context));
            const offset2:uint = source.child ? as3flatbuffers.Pack.reserveOffset(context, 2) : 0;
            const offset3:uint = source.next ? as3flatbuffers.Pack.reserveOffset(context, 3) : 0;
            const offset4:uint = source.values.length ? as3flatbuffers.Pack.reserveOffset(context, 4) : 0;
            const table:uint = as3flatbuffers.Pack.endTable(context);
            if (offset0)
            {
                as3flatbuffers.Pack.prepare(context, 4);
                as3flatbuffers.Pack.writeString(context, offset0, source.title);
            }
            if (offset2)
                as3flatbuffers.Pack.patchOffset(context, offset2, fixtures.framing.Child.packInto(source.child, context));
            if (offset3)
                as3flatbuffers.Pack.patchOffset(context, offset3, fixtures.framing.Record.packInto(source.next, context));
            if (offset4)
            {
                as3flatbuffers.Pack.prepareVector(context, 4, source.values.length, 4);
                const vector4:uint = as3flatbuffers.Pack.startVector(context, source.values.length);
                as3flatbuffers.Pack.patchOffset(context, offset4, vector4);
                const data4:uint = as3flatbuffers.Pack.reserve(context, source.values.length * 4);
                for (var index4:uint = 0; index4 < source.values.length; index4++)
                {
                    si32(source.values[index4], data4 + index4 * 4);
                }
            }
            return table;
        }

        /** Decode a FlatBuffer whose root-offset word starts at offset. */
        public static function unpack(bytes:flash.utils.ByteArray, destination:Record = null, offset:uint = 0):Record
        {
            if (!hasIdentifier(bytes, offset, false))
                throw new RangeError("Missing or incorrect file identifier");
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.begin(context, bytes);
            try
            {
                destination = unpackFrom(context, as3flatbuffers.Unpack.root(context, offset), destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }

        /** Internal generated entry point; context must already own domain memory. */
        public static function unpackFrom(context:as3flatbuffers.UnpackContext, base:uint, destination:Record):Record
        {
            const vtable:uint = as3flatbuffers.Unpack.vtable(context, base);
            const vtableSize:uint = li16(vtable);
            const objectSize:uint = li16(vtable + 2);
            if (!destination)
                destination = new Record();

            const position0:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 4, 4);
            if (!(position0))
                throw new RangeError("Required field title is missing");
            destination.title = as3flatbuffers.Unpack.stringValue(context, position0);

            const position1:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 6, 16);
            destination.aligned = position1 ? fixtures.framing.Aligned.unpackFrom(context, position1, destination.aligned) : null;

            const field2:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 8, 4);
            const position2:uint = as3flatbuffers.Unpack.tableOffset(context, field2);
            destination.child = position2 ? fixtures.framing.Child.unpackFrom(context, position2, destination.child) : null;

            const field3:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 10, 4);
            const position3:uint = as3flatbuffers.Unpack.tableOffset(context, field3);
            destination.next = position3 ? fixtures.framing.Record.unpackFrom(context, position3, destination.next) : null;

            const field4:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 12, 4);
            const vector4:uint = as3flatbuffers.Unpack.vector(context, field4, 4);
            const count4:uint = vector4 ? uint(li32(vector4)) : 0;
            destination.values.length = count4;

            for (var index4:uint = 0; index4 < count4; index4++)
            {
                const element4:uint = vector4 + 4 + index4 * 4;
                destination.values[index4] = uint(li32(element4));
            }
            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function packSizePrefixed(source:Record, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.beginRoot(context, dst, true, true, 827150918);
                const root:uint = packInto(source, context);
                as3flatbuffers.Pack.finishRoot(context, root, true);
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Decode one size-prefixed frame at offset; references are bounded to its declared length. */
        public static function unpackSizePrefixed(bytes:flash.utils.ByteArray, destination:Record = null, offset:uint = 0):Record
        {
            if (!hasIdentifier(bytes, offset, true))
                throw new RangeError("Missing or incorrect file identifier");
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.beginSizePrefixed(context, bytes, offset);
            try
            {
                destination = unpackFrom(context, as3flatbuffers.Unpack.root(context, 4), destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }
    }
}
