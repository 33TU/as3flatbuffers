// Code generated by as3flatc. DO NOT EDIT.
package fixtures.framing
{
    import as3flatbuffers.TableView;
    import flash.utils.ByteArray;
    import fixtures.framing.Aligned;
    import fixtures.framing.AlignedView;
    import fixtures.framing.Child;
    import fixtures.framing.ChildView;
    import fixtures.framing.Record;
    import fixtures.framing.RecordView;

    /** Borrowed read-only view; unpack() produces independent owned values. */
    public final class RecordView extends as3flatbuffers.TableView
    {
        private const alignedView:fixtures.framing.AlignedView = new fixtures.framing.AlignedView();

        private var childView:fixtures.framing.ChildView;

        private var nextView:fixtures.framing.RecordView;

        public function get title():String
        {
            const value:String = stringValue(4);
            if (value == null)
                throw new RangeError("Required field title is missing");
            return value;
        }

        public function get aligned():fixtures.framing.AlignedView
        {
            const position:uint = fieldOffset(6, 16);
            return position ? this.alignedView.bind(bytes, position) : null;
        }

        public function get child():fixtures.framing.ChildView
        {
            const position:uint = tableOffset(8);
            if (!position)
                return null;

            if (!this.childView)
                this.childView = new fixtures.framing.ChildView();

            this.childView.bind(bytes, position);
            return this.childView;
        }

        public function get next():fixtures.framing.RecordView
        {
            const position:uint = tableOffset(10);
            if (!position)
                return null;

            if (!this.nextView)
                this.nextView = new fixtures.framing.RecordView();

            this.nextView.bind(bytes, position);
            return this.nextView;
        }

        public function get valuesLength():uint
        {
            const position:uint = vectorOffset(12, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function values(index:uint):uint
        {
            const vector:uint = vectorOffset(12, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }
    }
}
