// Code generated by as3flatc. DO NOT EDIT.
package fixtures
{
    import as3flatbuffers.Pack;
    import as3flatbuffers.Unpack;
    import as3flatbuffers.UnpackContext;
    import avm2.intrinsics.memory.*;
    import flash.utils.ByteArray;

    /** Owned mutable value. pack() writes a complete FlatBuffer into the destination. */
    public final class SpecialFloats
    {
        private static const PACK:as3flatbuffers.PackContext = new as3flatbuffers.PackContext();
        private static const UNPACK:as3flatbuffers.UnpackContext = new as3flatbuffers.UnpackContext();

        public var f32:Number = NaN;
        public var f64:Number = Number.POSITIVE_INFINITY;
        public var negative:Number = Number.NEGATIVE_INFINITY;

        public static function reset(msg:SpecialFloats):void
        {
            msg.f32 = NaN;
            msg.f64 = Number.POSITIVE_INFINITY;
            msg.negative = Number.NEGATIVE_INFINITY;
        }

        public static function clone(source:SpecialFloats):SpecialFloats
        {
            if (!source)
                return null;

            const destination:SpecialFloats = new SpecialFloats();
            destination.f32 = source.f32;
            destination.f64 = source.f64;
            destination.negative = source.negative;

            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function pack(source:SpecialFloats, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.begin(context, dst, true);
                as3flatbuffers.Pack.finish(context, packInto(source, context));
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Write into an active context and return the absolute object offset. */
        public static function packInto(source:SpecialFloats, context:as3flatbuffers.PackContext):uint
        {
            if (!source || !context)
                throw new ArgumentError("Source and context must be non-null");

            as3flatbuffers.Pack.ensure(context, 59);
            as3flatbuffers.Pack.prepare(context, 2);
            as3flatbuffers.Pack.reserveVtable(context, 3);
            as3flatbuffers.Pack.prepare(context, 8);
            as3flatbuffers.Pack.startTable(context);
            as3flatbuffers.Pack.addFloat32(context, 0, source.f32);
            if (source.f64 != Number.POSITIVE_INFINITY)
            {
                as3flatbuffers.Pack.prepare(context, 8);
                as3flatbuffers.Pack.addFloat64(context, 1, source.f64);
            }
            if (source.negative != Number.NEGATIVE_INFINITY)
            {
                as3flatbuffers.Pack.prepare(context, 8);
                as3flatbuffers.Pack.addFloat64(context, 2, source.negative);
            }
            return as3flatbuffers.Pack.endTable(context);
        }

        /** Decode a FlatBuffer whose root-offset word starts at offset. */
        public static function unpack(bytes:flash.utils.ByteArray, destination:SpecialFloats = null, offset:uint = 0):SpecialFloats
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.begin(context, bytes);
            try
            {
                destination = unpackFrom(context, as3flatbuffers.Unpack.root(context, offset), destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }

        /** Internal generated entry point; context must already own domain memory. */
        public static function unpackFrom(context:as3flatbuffers.UnpackContext, base:uint, destination:SpecialFloats):SpecialFloats
        {
            const vtable:uint = as3flatbuffers.Unpack.vtable(context, base);
            const vtableSize:uint = li16(vtable);
            const objectSize:uint = li16(vtable + 2);
            if (!destination)
                destination = new SpecialFloats();

            const position0:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 4, 4);
            if (!position0)
            {
                destination.f32 = NaN;
            }
            else
            {
                destination.f32 = lf32(position0);
            }

            const position1:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 6, 8);
            if (!position1)
            {
                destination.f64 = Number.POSITIVE_INFINITY;
            }
            else
            {
                destination.f64 = lf64(position1);
            }

            const position2:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 8, 8);
            if (!position2)
            {
                destination.negative = Number.NEGATIVE_INFINITY;
            }
            else
            {
                destination.negative = lf64(position2);
            }
            return destination;
        }
    }
}
