// Code generated by as3flatc. DO NOT EDIT.
package fixtures.required
{
    import as3flatbuffers.Pack;
    import as3flatbuffers.Unpack;
    import as3flatbuffers.UnpackContext;
    import avm2.intrinsics.memory.*;
    import flash.utils.ByteArray;
    import fixtures.required.Child;
    import fixtures.required.Payload;
    import fixtures.required.Position;

    /** Owned mutable value. pack() writes a complete FlatBuffer into the destination. */
    public final class RequiredFields
    {
        private static const PACK:as3flatbuffers.PackContext = new as3flatbuffers.PackContext();
        private static const UNPACK:as3flatbuffers.UnpackContext = new as3flatbuffers.UnpackContext();

        public var name:String = null;
        public var position:fixtures.required.Position = null;
        public var child:fixtures.required.Child = null;
        public var numbers:Vector.<int> = new Vector.<int>();
        public var names:Vector.<String> = new Vector.<String>();
        public var positions:Vector.<fixtures.required.Position> = new Vector.<fixtures.required.Position>();
        public var children:Vector.<fixtures.required.Child> = new Vector.<fixtures.required.Child>();
        public var payload:fixtures.required.Payload = new fixtures.required.Payload();
        public var payloads:Vector.<fixtures.required.Payload> = new Vector.<fixtures.required.Payload>();
        public var optionalName:String = null;

        public static function reset(msg:RequiredFields):void
        {
            msg.name = null;
            msg.position = null;
            msg.child = null;
            msg.numbers.length = 0;
            msg.names.length = 0;
            msg.positions.length = 0;
            msg.children.length = 0;
            fixtures.required.Payload.reset(msg.payload);
            msg.payloads.length = 0;
            msg.optionalName = null;
        }

        public static function clone(source:RequiredFields):RequiredFields
        {
            if (!source)
                return null;

            const destination:RequiredFields = new RequiredFields();
            destination.name = source.name;
            destination.position = fixtures.required.Position.clone(source.position);
            destination.child = fixtures.required.Child.clone(source.child);
            destination.numbers = source.numbers.concat();
            destination.names = source.names.concat();
            destination.positions.length = source.positions.length;
            for (var index5:uint = 0; index5 < source.positions.length; index5++)
            {
                destination.positions[index5] = fixtures.required.Position.clone(source.positions[index5]);
            }
            destination.children.length = source.children.length;
            for (var index6:uint = 0; index6 < source.children.length; index6++)
            {
                destination.children[index6] = fixtures.required.Child.clone(source.children[index6]);
            }
            destination.payload = fixtures.required.Payload.clone(source.payload);
            destination.payloads.length = source.payloads.length;
            for (var index10:uint = 0; index10 < source.payloads.length; index10++)
            {
                destination.payloads[index10] = fixtures.required.Payload.clone(source.payloads[index10]);
            }
            destination.optionalName = source.optionalName;

            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function pack(source:RequiredFields, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.begin(context, dst, true);
                as3flatbuffers.Pack.finish(context, packInto(source, context));
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Write into an active context and return the absolute object offset. */
        public static function packInto(source:RequiredFields, context:as3flatbuffers.PackContext):uint
        {
            if (!source || !context)
                throw new ArgumentError("Source and context must be non-null");

            if (source.name == null)
                throw new ArgumentError("Required field name must be set");
            if (source.position == null)
                throw new ArgumentError("Required field position must be set");
            if (source.child == null)
                throw new ArgumentError("Required field child must be set");
            if (source.numbers == null)
                throw new ArgumentError("Required field numbers must be set");
            if (source.names == null)
                throw new ArgumentError("Required field names must be set");
            if (source.positions == null)
                throw new ArgumentError("Required field positions must be set");
            if (source.children == null)
                throw new ArgumentError("Required field children must be set");
            if (source.payload == null || source.payload.type == 0)
                throw new ArgumentError("Required field payload must be set");
            if (source.payloads == null)
                throw new ArgumentError("Required field payloads must be set");
            as3flatbuffers.Pack.ensure(context, 115);
            as3flatbuffers.Pack.prepare(context, 2);
            as3flatbuffers.Pack.reserveVtable(context, 12);
            as3flatbuffers.Pack.prepare(context, 4);
            as3flatbuffers.Pack.startTable(context);
            const offset0:uint = as3flatbuffers.Pack.reserveOffset(context, 0);
            if (source.position)
                as3flatbuffers.Pack.addStruct(context, 1, fixtures.required.Position.packInto(source.position, context));
            const offset2:uint = as3flatbuffers.Pack.reserveOffset(context, 2);
            const offset3:uint = as3flatbuffers.Pack.reserveOffset(context, 3);
            const offset4:uint = as3flatbuffers.Pack.reserveOffset(context, 4);
            const offset5:uint = as3flatbuffers.Pack.reserveOffset(context, 5);
            const offset6:uint = as3flatbuffers.Pack.reserveOffset(context, 6);
            fixtures.required.Payload.validate(source.payload);
            var offset8:uint = 0;
            if (source.payload.type)
            {
                as3flatbuffers.Pack.addUint8(context, 7, source.payload.type);
                as3flatbuffers.Pack.prepare(context, 4);
                offset8 = as3flatbuffers.Pack.reserveOffset(context, 8);
            }
            as3flatbuffers.Pack.prepare(context, 4);
            const tagsOffset10:uint = as3flatbuffers.Pack.reserveOffset(context, 9);
            const offset10:uint = as3flatbuffers.Pack.reserveOffset(context, 10);
            var offset11:uint = 0;
            if (source.optionalName != null)
            {
                as3flatbuffers.Pack.prepare(context, 4);
                offset11 = as3flatbuffers.Pack.reserveOffset(context, 11);
            }
            const table:uint = as3flatbuffers.Pack.endTable(context);
            if (offset0)
            {
                as3flatbuffers.Pack.prepare(context, 4);
                as3flatbuffers.Pack.writeString(context, offset0, source.name);
            }
            if (offset2)
                as3flatbuffers.Pack.patchOffset(context, offset2, fixtures.required.Child.packInto(source.child, context));
            if (offset3)
            {
                as3flatbuffers.Pack.prepareVector(context, 4, source.numbers.length, 4);
                const vector3:uint = as3flatbuffers.Pack.startVector(context, source.numbers.length);
                as3flatbuffers.Pack.patchOffset(context, offset3, vector3);
                const data3:uint = as3flatbuffers.Pack.reserve(context, source.numbers.length * 4);
                for (var index3:uint = 0; index3 < source.numbers.length; index3++)
                {
                    si32(source.numbers[index3], data3 + index3 * 4);
                }
            }
            if (offset4)
            {
                as3flatbuffers.Pack.prepareVector(context, 4, source.names.length, 4);
                const vector4:uint = as3flatbuffers.Pack.startVector(context, source.names.length);
                as3flatbuffers.Pack.patchOffset(context, offset4, vector4);
                const data4:uint = as3flatbuffers.Pack.reserve(context, source.names.length * 4);
                for (var index4:uint = 0; index4 < source.names.length; index4++)
                {
                    if (source.names[index4] == null)
                        throw new ArgumentError("names elements must be non-null");
                    as3flatbuffers.Pack.prepare(context, 4);
                    as3flatbuffers.Pack.writeString(context, data4 + index4 * 4, source.names[index4]);
                }
            }
            if (offset5)
            {
                as3flatbuffers.Pack.prepareVector(context, 4, source.positions.length, 8);
                const vector5:uint = as3flatbuffers.Pack.startVector(context, source.positions.length);
                as3flatbuffers.Pack.patchOffset(context, offset5, vector5);
                const data5:uint = as3flatbuffers.Pack.reserve(context, source.positions.length * 8);
                for (var index5:uint = 0; index5 < source.positions.length; index5++)
                {
                    if (source.positions[index5] == null)
                        throw new ArgumentError("positions elements must be non-null");
                    const element5:uint = data5 + index5 * 8;
                    sf32(source.positions[index5].x, element5 + 0);
                    sf32(source.positions[index5].y, element5 + 4);
                }
            }
            if (offset6)
            {
                as3flatbuffers.Pack.prepareVector(context, 4, source.children.length, 4);
                const vector6:uint = as3flatbuffers.Pack.startVector(context, source.children.length);
                as3flatbuffers.Pack.patchOffset(context, offset6, vector6);
                const data6:uint = as3flatbuffers.Pack.reserve(context, source.children.length * 4);
                for (var index6:uint = 0; index6 < source.children.length; index6++)
                {
                    if (source.children[index6] == null)
                        throw new ArgumentError("children elements must be non-null");
                    const child6:uint = fixtures.required.Child.packInto(source.children[index6], context);
                    as3flatbuffers.Pack.patchOffset(context, data6 + index6 * 4, child6);
                }
            }
            if (offset8)
            {
                fixtures.required.Payload.packInto(source.payload, context, offset8);
            }
            if (offset10)
            {
                const count10:uint = source.payloads.length;
                as3flatbuffers.Pack.prepareVector(context, 1, count10, 1);
                const tags10:uint = as3flatbuffers.Pack.startVector(context, count10);
                as3flatbuffers.Pack.patchOffset(context, tagsOffset10, tags10);
                const tagData10:uint = as3flatbuffers.Pack.reserve(context, count10);
                for (var tagIndex10:uint = 0; tagIndex10 < count10; tagIndex10++)
                {
                    fixtures.required.Payload.validate(source.payloads[tagIndex10]);
                    si8(source.payloads[tagIndex10].type, tagData10 + tagIndex10);
                }
                as3flatbuffers.Pack.prepareVector(context, 4, count10, 4);
                const vector10:uint = as3flatbuffers.Pack.startVector(context, count10);
                as3flatbuffers.Pack.patchOffset(context, offset10, vector10);
                const data10:uint = as3flatbuffers.Pack.reserve(context, count10 * 4);
                for (var index10:uint = 0; index10 < count10; index10++)
                {
                    const reference10:uint = data10 + index10 * 4;
                    si32(0, reference10);
                    if (source.payloads[index10].type)
                        fixtures.required.Payload.packInto(source.payloads[index10], context, reference10);
                }
            }
            if (offset11)
            {
                as3flatbuffers.Pack.prepare(context, 4);
                as3flatbuffers.Pack.writeString(context, offset11, source.optionalName);
            }
            return table;
        }

        /** Decode a FlatBuffer whose root-offset word starts at offset. */
        public static function unpack(bytes:flash.utils.ByteArray, destination:RequiredFields = null, offset:uint = 0):RequiredFields
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.begin(context, bytes);
            try
            {
                destination = unpackFrom(context, as3flatbuffers.Unpack.root(context, offset), destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }

        /** Internal generated entry point; context must already own domain memory. */
        public static function unpackFrom(context:as3flatbuffers.UnpackContext, base:uint, destination:RequiredFields):RequiredFields
        {
            const vtable:uint = as3flatbuffers.Unpack.vtable(context, base);
            const vtableSize:uint = li16(vtable);
            const objectSize:uint = li16(vtable + 2);
            if (!destination)
                destination = new RequiredFields();

            const position0:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 4, 4);
            if (!(position0))
                throw new RangeError("Required field name is missing");
            destination.name = as3flatbuffers.Unpack.stringValue(context, position0);

            const position1:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 6, 8);
            if (!(position1))
                throw new RangeError("Required field position is missing");
            destination.position = position1 ? fixtures.required.Position.unpackFrom(context, position1, destination.position) : null;

            const field2:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 8, 4);
            if (!(field2))
                throw new RangeError("Required field child is missing");
            const position2:uint = as3flatbuffers.Unpack.tableOffset(context, field2);
            destination.child = position2 ? fixtures.required.Child.unpackFrom(context, position2, destination.child) : null;

            const field3:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 10, 4);
            if (!(field3))
                throw new RangeError("Required field numbers is missing");
            const vector3:uint = as3flatbuffers.Unpack.vector(context, field3, 4);
            const count3:uint = vector3 ? uint(li32(vector3)) : 0;
            destination.numbers.length = count3;

            for (var index3:uint = 0; index3 < count3; index3++)
            {
                const element3:uint = vector3 + 4 + index3 * 4;
                destination.numbers[index3] = li32(element3);
            }

            const field4:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 12, 4);
            if (!(field4))
                throw new RangeError("Required field names is missing");
            const vector4:uint = as3flatbuffers.Unpack.vector(context, field4, 4);
            const count4:uint = vector4 ? uint(li32(vector4)) : 0;
            destination.names.length = count4;

            for (var index4:uint = 0; index4 < count4; index4++)
            {
                const element4:uint = vector4 + 4 + index4 * 4;
                destination.names[index4] = as3flatbuffers.Unpack.stringValue(context, element4);
            }

            const field5:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 14, 4);
            if (!(field5))
                throw new RangeError("Required field positions is missing");
            const vector5:uint = as3flatbuffers.Unpack.vector(context, field5, 8);
            const count5:uint = vector5 ? uint(li32(vector5)) : 0;
            destination.positions.length = count5;

            for (var index5:uint = 0; index5 < count5; index5++)
            {
                const element5:uint = vector5 + 4 + index5 * 8;
                destination.positions[index5] = fixtures.required.Position.unpackFrom(context, element5, destination.positions[index5]);
            }

            const field6:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 16, 4);
            if (!(field6))
                throw new RangeError("Required field children is missing");
            const vector6:uint = as3flatbuffers.Unpack.vector(context, field6, 4);
            const count6:uint = vector6 ? uint(li32(vector6)) : 0;
            destination.children.length = count6;

            for (var index6:uint = 0; index6 < count6; index6++)
            {
                const element6:uint = vector6 + 4 + index6 * 4;
                const child6:uint = as3flatbuffers.Unpack.tableOffset(context, element6);
                destination.children[index6] = fixtures.required.Child.unpackFrom(context, child6, destination.children[index6]);
            }

            const tag8:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 18, 1);
            const field8:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 20, 4);
            if (!(field8))
                throw new RangeError("Required field payload is missing");
            if (!(tag8 && li8(tag8)))
                throw new RangeError("Required field payload is missing");
            destination.payload = fixtures.required.Payload.unpackFrom(context, tag8 ? li8(tag8) : 0, field8, destination.payload);

            const tagsField10:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 22, 4);
            const field10:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 24, 4);
            if (!(tagsField10 && field10))
                throw new RangeError("Required field payloads is missing");
            const tags10:uint = as3flatbuffers.Unpack.vector(context, tagsField10, 1);
            const vector10:uint = as3flatbuffers.Unpack.vector(context, field10, 4);
            const tagCount10:uint = tags10 ? uint(li32(tags10)) : 0;
            const count10:uint = vector10 ? uint(li32(vector10)) : 0;
            if ((tags10 == 0) != (vector10 == 0) || tagCount10 != count10)
                throw new RangeError("Union tag and payload vectors must match");
            destination.payloads.length = count10;
            for (var index10:uint = 0; index10 < count10; index10++)
            {
                destination.payloads[index10] = fixtures.required.Payload.unpackFrom(context, li8(tags10 + 4 + index10), vector10 + 4 + index10 * 4, destination.payloads[index10]);
            }

            const position11:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 26, 4);
            destination.optionalName = as3flatbuffers.Unpack.stringValue(context, position11);
            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function packSizePrefixed(source:RequiredFields, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.beginRoot(context, dst, true, false, 0);
                const root:uint = packInto(source, context);
                as3flatbuffers.Pack.finishRoot(context, root, true);
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Decode one size-prefixed frame at offset; references are bounded to its declared length. */
        public static function unpackSizePrefixed(bytes:flash.utils.ByteArray, destination:RequiredFields = null, offset:uint = 0):RequiredFields
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.beginSizePrefixed(context, bytes, offset);
            try
            {
                destination = unpackFrom(context, as3flatbuffers.Unpack.root(context, 4), destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }
    }
}
