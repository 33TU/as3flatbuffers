// Code generated by as3flatc. DO NOT EDIT.
package fixtures
{
    import as3flatbuffers.TableView;
    import flash.utils.ByteArray;
    import fixtures.geometry.Aligned;
    import fixtures.geometry.AlignedView;
    import fixtures.geometry.Envelope;
    import fixtures.geometry.EnvelopeView;
    import fixtures.geometry.Frame;
    import fixtures.geometry.FrameView;
    import fixtures.geometry.Point;
    import fixtures.geometry.PointView;

    /** Borrowed read-only view; unpack() produces independent owned values. */
    public final class InlineRootView extends as3flatbuffers.TableView
    {
        private var pointView_:fixtures.geometry.PointView;

        private var frameView:fixtures.geometry.FrameView;

        private var envelopeView:fixtures.geometry.EnvelopeView;

        private var alignedView:fixtures.geometry.AlignedView;

        public function bind(input:flash.utils.ByteArray, rootOffset:uint = 0):InlineRootView
        {
            bindRoot(input, rootOffset);
            return this;
        }

        public function get point():fixtures.geometry.PointView
        {
            const position:uint = field(0, 8);
            return position ? new fixtures.geometry.PointView().bind(bytes, position) : null;
        }

        public function get frame():fixtures.geometry.FrameView
        {
            const position:uint = field(1, 56);
            return position ? new fixtures.geometry.FrameView().bind(bytes, position) : null;
        }

        public function get envelope():fixtures.geometry.EnvelopeView
        {
            const position:uint = field(2, 72);
            return position ? new fixtures.geometry.EnvelopeView().bind(bytes, position) : null;
        }

        public function get aligned():fixtures.geometry.AlignedView
        {
            const position:uint = field(3, 16);
            return position ? new fixtures.geometry.AlignedView().bind(bytes, position) : null;
        }

        public function get label_():int
        {
            return int32(4, 0);
        }

        public function get pointView():int
        {
            return int32(5, 0);
        }

        public function unpack(destination:InlineRoot = null):InlineRoot
        {
            if (!destination) destination = new InlineRoot();
            const pointView_Position:uint = field(0, 8);
            if (!pointView_Position) destination.point = null;
            else
            {
                if (!this.pointView_) this.pointView_ = new fixtures.geometry.PointView();
                destination.point = this.pointView_.bind(bytes, pointView_Position).unpack(destination.point);
            }
            const frameViewPosition:uint = field(1, 56);
            if (!frameViewPosition) destination.frame = null;
            else
            {
                if (!this.frameView) this.frameView = new fixtures.geometry.FrameView();
                destination.frame = this.frameView.bind(bytes, frameViewPosition).unpack(destination.frame);
            }
            const envelopeViewPosition:uint = field(2, 72);
            if (!envelopeViewPosition) destination.envelope = null;
            else
            {
                if (!this.envelopeView) this.envelopeView = new fixtures.geometry.EnvelopeView();
                destination.envelope = this.envelopeView.bind(bytes, envelopeViewPosition).unpack(destination.envelope);
            }
            const alignedViewPosition:uint = field(3, 16);
            if (!alignedViewPosition) destination.aligned = null;
            else
            {
                if (!this.alignedView) this.alignedView = new fixtures.geometry.AlignedView();
                destination.aligned = this.alignedView.bind(bytes, alignedViewPosition).unpack(destination.aligned);
            }
            destination.label_ = this.label_;
            destination.pointView = this.pointView;
            return destination;
        }
    }
}
