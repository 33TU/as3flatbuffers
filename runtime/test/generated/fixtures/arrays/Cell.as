// Code generated by as3flatc. DO NOT EDIT.
package fixtures.arrays
{
    import as3flatbuffers.Pack;
    import as3flatbuffers.Unpack;
    import as3flatbuffers.UnpackContext;
    import avm2.intrinsics.memory.*;
    import flash.utils.ByteArray;

    /** Owned inline struct. pack() writes raw struct bytes into the destination. */
    public final class Cell
    {
        private static const PACK:as3flatbuffers.PackContext = new as3flatbuffers.PackContext();
        private static const UNPACK:as3flatbuffers.UnpackContext = new as3flatbuffers.UnpackContext();

        public var x:int = 0;
        public var samples:Vector.<Number> = new Vector.<Number>(3, true);

        public function Cell()
        {
            for (var index1:uint = 0; index1 < 3; index1++)
            {
                this.samples[index1] = 0;
            }
        }

        public static function reset(msg:Cell):void
        {
            msg.x = 0;
            for (var index1:uint = 0; index1 < 3; index1++)
            {
                msg.samples[index1] = 0;
            }
        }

        public static function clone(source:Cell):Cell
        {
            if (!source)
                return null;

            const destination:Cell = new Cell();
            destination.x = source.x;
            for (var index1:uint = 0; index1 < 3; index1++)
            {
                destination.samples[index1] = source.samples[index1];
            }

            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function pack(source:Cell, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.begin(context, dst, false);
                as3flatbuffers.Pack.finish(context, packInto(source, context));
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Write into an active context and return the absolute object offset. */
        public static function packInto(source:Cell, context:as3flatbuffers.PackContext):uint
        {
            if (!source || !context)
                throw new ArgumentError("Source and context must be non-null");

            const start:uint = as3flatbuffers.Pack.prepareStruct(context, 16, 16);
            si16(source.x, start + 0);
            si16(0, start + 2);
            if (!source.samples || source.samples.length != 3)
                throw new ArgumentError("samples must contain 3 elements");
            for (var startArrayIndex0:uint = 0; startArrayIndex0 < 3; startArrayIndex0++)
            {
                const startArrayPosition0:uint = start + 4 + startArrayIndex0 * 4;
                sf32(source.samples[startArrayIndex0], startArrayPosition0);
            }
            return start;
        }

        /** Decode raw struct bytes at offset into a new or reused owned value. */
        public static function unpack(bytes:flash.utils.ByteArray, destination:Cell = null, offset:uint = 0):Cell
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.begin(context, bytes);
            try
            {
                destination = unpackFrom(context, offset, destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }

        /** Internal generated entry point; context must already own domain memory. */
        public static function unpackFrom(context:as3flatbuffers.UnpackContext, base:uint, destination:Cell):Cell
        {
            as3flatbuffers.Unpack.struct(context, base, 16);
            if (!destination)
                destination = new Cell();

            destination.x = sxi16(li16(base + 0));

            for (var index1:uint = 0; index1 < 3; index1++)
            {
                const element1:uint = base + 4 + index1 * 4;
                destination.samples[index1] = lf32(element1);
            }
            return destination;
        }
    }
}
