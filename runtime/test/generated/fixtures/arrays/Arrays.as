// Code generated by as3flatc. DO NOT EDIT.
package fixtures.arrays
{
    import as3flatbuffers.Pack;
    import as3flatbuffers.Unpack;
    import as3flatbuffers.UnpackContext;
    import avm2.intrinsics.memory.*;
    import flash.utils.ByteArray;
    import as3flatbuffers.types.Int64;
    import as3flatbuffers.types.UInt64;
    import fixtures.arrays.Cell;

    /** Owned inline struct. pack() writes raw struct bytes into the destination. */
    public final class Arrays
    {
        private static const PACK:as3flatbuffers.PackContext = new as3flatbuffers.PackContext();
        private static const UNPACK:as3flatbuffers.UnpackContext = new as3flatbuffers.UnpackContext();

        public var flags:Vector.<Boolean> = new Vector.<Boolean>(3, true);
        public var tiny:Vector.<int> = new Vector.<int>(3, true);
        public var octets:Vector.<uint> = new Vector.<uint>(3, true);
        public var small:Vector.<int> = new Vector.<int>(3, true);
        public var shorts:Vector.<uint> = new Vector.<uint>(3, true);
        public var ints:Vector.<int> = new Vector.<int>(3, true);
        public var uints:Vector.<uint> = new Vector.<uint>(3, true);
        public var longs:Vector.<as3flatbuffers.types.Int64> = new Vector.<as3flatbuffers.types.Int64>(3, true);
        public var ulongs:Vector.<as3flatbuffers.types.UInt64> = new Vector.<as3flatbuffers.types.UInt64>(3, true);
        public var floats:Vector.<Number> = new Vector.<Number>(3, true);
        public var doubles:Vector.<Number> = new Vector.<Number>(3, true);
        public var modes:Vector.<uint> = new Vector.<uint>(3, true);
        public var cells:Vector.<fixtures.arrays.Cell> = new Vector.<fixtures.arrays.Cell>(2, true);
        public var tail:uint = 0;

        public function Arrays()
        {
            for (var index0:uint = 0; index0 < 3; index0++)
            {
                this.flags[index0] = false;
            }
            for (var index1:uint = 0; index1 < 3; index1++)
            {
                this.tiny[index1] = 0;
            }
            for (var index2:uint = 0; index2 < 3; index2++)
            {
                this.octets[index2] = 0;
            }
            for (var index3:uint = 0; index3 < 3; index3++)
            {
                this.small[index3] = 0;
            }
            for (var index4:uint = 0; index4 < 3; index4++)
            {
                this.shorts[index4] = 0;
            }
            for (var index5:uint = 0; index5 < 3; index5++)
            {
                this.ints[index5] = 0;
            }
            for (var index6:uint = 0; index6 < 3; index6++)
            {
                this.uints[index6] = 0;
            }
            for (var index7:uint = 0; index7 < 3; index7++)
            {
                this.longs[index7] = new as3flatbuffers.types.Int64();
            }
            for (var index8:uint = 0; index8 < 3; index8++)
            {
                this.ulongs[index8] = new as3flatbuffers.types.UInt64();
            }
            for (var index9:uint = 0; index9 < 3; index9++)
            {
                this.floats[index9] = 0;
            }
            for (var index10:uint = 0; index10 < 3; index10++)
            {
                this.doubles[index10] = 0;
            }
            for (var index11:uint = 0; index11 < 3; index11++)
            {
                this.modes[index11] = 0;
            }
            for (var index12:uint = 0; index12 < 2; index12++)
            {
                this.cells[index12] = new fixtures.arrays.Cell();
            }
        }

        public static function reset(msg:Arrays):void
        {
            for (var index0:uint = 0; index0 < 3; index0++)
            {
                msg.flags[index0] = false;
            }
            for (var index1:uint = 0; index1 < 3; index1++)
            {
                msg.tiny[index1] = 0;
            }
            for (var index2:uint = 0; index2 < 3; index2++)
            {
                msg.octets[index2] = 0;
            }
            for (var index3:uint = 0; index3 < 3; index3++)
            {
                msg.small[index3] = 0;
            }
            for (var index4:uint = 0; index4 < 3; index4++)
            {
                msg.shorts[index4] = 0;
            }
            for (var index5:uint = 0; index5 < 3; index5++)
            {
                msg.ints[index5] = 0;
            }
            for (var index6:uint = 0; index6 < 3; index6++)
            {
                msg.uints[index6] = 0;
            }
            for (var index7:uint = 0; index7 < 3; index7++)
            {
                msg.longs[index7].reset();
            }
            for (var index8:uint = 0; index8 < 3; index8++)
            {
                msg.ulongs[index8].reset();
            }
            for (var index9:uint = 0; index9 < 3; index9++)
            {
                msg.floats[index9] = 0;
            }
            for (var index10:uint = 0; index10 < 3; index10++)
            {
                msg.doubles[index10] = 0;
            }
            for (var index11:uint = 0; index11 < 3; index11++)
            {
                msg.modes[index11] = 0;
            }
            for (var index12:uint = 0; index12 < 2; index12++)
            {
                fixtures.arrays.Cell.reset(msg.cells[index12]);
            }
            msg.tail = 0;
        }

        public static function clone(source:Arrays):Arrays
        {
            if (!source)
                return null;

            const destination:Arrays = new Arrays();
            for (var index0:uint = 0; index0 < 3; index0++)
            {
                destination.flags[index0] = source.flags[index0];
            }
            for (var index1:uint = 0; index1 < 3; index1++)
            {
                destination.tiny[index1] = source.tiny[index1];
            }
            for (var index2:uint = 0; index2 < 3; index2++)
            {
                destination.octets[index2] = source.octets[index2];
            }
            for (var index3:uint = 0; index3 < 3; index3++)
            {
                destination.small[index3] = source.small[index3];
            }
            for (var index4:uint = 0; index4 < 3; index4++)
            {
                destination.shorts[index4] = source.shorts[index4];
            }
            for (var index5:uint = 0; index5 < 3; index5++)
            {
                destination.ints[index5] = source.ints[index5];
            }
            for (var index6:uint = 0; index6 < 3; index6++)
            {
                destination.uints[index6] = source.uints[index6];
            }
            for (var index7:uint = 0; index7 < 3; index7++)
            {
                destination.longs[index7].copyFrom(source.longs[index7]);
            }
            for (var index8:uint = 0; index8 < 3; index8++)
            {
                destination.ulongs[index8].copyFrom(source.ulongs[index8]);
            }
            for (var index9:uint = 0; index9 < 3; index9++)
            {
                destination.floats[index9] = source.floats[index9];
            }
            for (var index10:uint = 0; index10 < 3; index10++)
            {
                destination.doubles[index10] = source.doubles[index10];
            }
            for (var index11:uint = 0; index11 < 3; index11++)
            {
                destination.modes[index11] = source.modes[index11];
            }
            for (var index12:uint = 0; index12 < 2; index12++)
            {
                destination.cells[index12] = fixtures.arrays.Cell.clone(source.cells[index12]);
            }
            destination.tail = source.tail;

            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function pack(source:Arrays, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.begin(context, dst, false);
                as3flatbuffers.Pack.finish(context, packInto(source, context));
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Write into an active context and return the absolute object offset. */
        public static function packInto(source:Arrays, context:as3flatbuffers.PackContext):uint
        {
            if (!source || !context)
                throw new ArgumentError("Source and context must be non-null");

            const start:uint = as3flatbuffers.Pack.prepareStruct(context, 192, 16);
            if (!source.flags || source.flags.length != 3)
                throw new ArgumentError("flags must contain 3 elements");
            for (var startArrayIndex0:uint = 0; startArrayIndex0 < 3; startArrayIndex0++)
            {
                const startArrayPosition0:uint = start + 0 + startArrayIndex0 * 1;
                si8((source.flags[startArrayIndex0] ? 1 : 0), startArrayPosition0);
            }
            if (!source.tiny || source.tiny.length != 3)
                throw new ArgumentError("tiny must contain 3 elements");
            for (var startArrayIndex1:uint = 0; startArrayIndex1 < 3; startArrayIndex1++)
            {
                const startArrayPosition1:uint = start + 3 + startArrayIndex1 * 1;
                si8(source.tiny[startArrayIndex1], startArrayPosition1);
            }
            if (!source.octets || source.octets.length != 3)
                throw new ArgumentError("octets must contain 3 elements");
            for (var startArrayIndex2:uint = 0; startArrayIndex2 < 3; startArrayIndex2++)
            {
                const startArrayPosition2:uint = start + 6 + startArrayIndex2 * 1;
                si8(source.octets[startArrayIndex2], startArrayPosition2);
            }
            si8(0, start + 9);
            if (!source.small || source.small.length != 3)
                throw new ArgumentError("small must contain 3 elements");
            for (var startArrayIndex3:uint = 0; startArrayIndex3 < 3; startArrayIndex3++)
            {
                const startArrayPosition3:uint = start + 10 + startArrayIndex3 * 2;
                si16(source.small[startArrayIndex3], startArrayPosition3);
            }
            if (!source.shorts || source.shorts.length != 3)
                throw new ArgumentError("shorts must contain 3 elements");
            for (var startArrayIndex4:uint = 0; startArrayIndex4 < 3; startArrayIndex4++)
            {
                const startArrayPosition4:uint = start + 16 + startArrayIndex4 * 2;
                si16(source.shorts[startArrayIndex4], startArrayPosition4);
            }
            si16(0, start + 22);
            if (!source.ints || source.ints.length != 3)
                throw new ArgumentError("ints must contain 3 elements");
            for (var startArrayIndex5:uint = 0; startArrayIndex5 < 3; startArrayIndex5++)
            {
                const startArrayPosition5:uint = start + 24 + startArrayIndex5 * 4;
                si32(source.ints[startArrayIndex5], startArrayPosition5);
            }
            if (!source.uints || source.uints.length != 3)
                throw new ArgumentError("uints must contain 3 elements");
            for (var startArrayIndex6:uint = 0; startArrayIndex6 < 3; startArrayIndex6++)
            {
                const startArrayPosition6:uint = start + 36 + startArrayIndex6 * 4;
                si32(source.uints[startArrayIndex6], startArrayPosition6);
            }
            if (!source.longs || source.longs.length != 3)
                throw new ArgumentError("longs must contain 3 elements");
            for (var startArrayIndex7:uint = 0; startArrayIndex7 < 3; startArrayIndex7++)
            {
                const startArrayPosition7:uint = start + 48 + startArrayIndex7 * 8;
                if (!source.longs[startArrayIndex7])
                    throw new ArgumentError("longs elements must be non-null");
                si32(source.longs[startArrayIndex7].low, startArrayPosition7);
                si32(source.longs[startArrayIndex7].high, startArrayPosition7 + 4);
            }
            if (!source.ulongs || source.ulongs.length != 3)
                throw new ArgumentError("ulongs must contain 3 elements");
            for (var startArrayIndex8:uint = 0; startArrayIndex8 < 3; startArrayIndex8++)
            {
                const startArrayPosition8:uint = start + 72 + startArrayIndex8 * 8;
                if (!source.ulongs[startArrayIndex8])
                    throw new ArgumentError("ulongs elements must be non-null");
                si32(source.ulongs[startArrayIndex8].low, startArrayPosition8);
                si32(source.ulongs[startArrayIndex8].high, startArrayPosition8 + 4);
            }
            if (!source.floats || source.floats.length != 3)
                throw new ArgumentError("floats must contain 3 elements");
            for (var startArrayIndex9:uint = 0; startArrayIndex9 < 3; startArrayIndex9++)
            {
                const startArrayPosition9:uint = start + 96 + startArrayIndex9 * 4;
                sf32(source.floats[startArrayIndex9], startArrayPosition9);
            }
            si32(0, start + 108);
            if (!source.doubles || source.doubles.length != 3)
                throw new ArgumentError("doubles must contain 3 elements");
            for (var startArrayIndex10:uint = 0; startArrayIndex10 < 3; startArrayIndex10++)
            {
                const startArrayPosition10:uint = start + 112 + startArrayIndex10 * 8;
                sf64(source.doubles[startArrayIndex10], startArrayPosition10);
            }
            if (!source.modes || source.modes.length != 3)
                throw new ArgumentError("modes must contain 3 elements");
            for (var startArrayIndex11:uint = 0; startArrayIndex11 < 3; startArrayIndex11++)
            {
                const startArrayPosition11:uint = start + 136 + startArrayIndex11 * 1;
                si8(source.modes[startArrayIndex11], startArrayPosition11);
            }
            si32(0, start + 139);
            si8(0, start + 143);
            if (!source.cells || source.cells.length != 2)
                throw new ArgumentError("cells must contain 2 elements");
            for (var startArrayIndex12:uint = 0; startArrayIndex12 < 2; startArrayIndex12++)
            {
                const startArrayPosition12:uint = start + 144 + startArrayIndex12 * 16;
                if (!source.cells[startArrayIndex12])
                    throw new ArgumentError("cells elements must be non-null");
                si16(source.cells[startArrayIndex12].x, startArrayPosition12 + 0);
                si16(0, startArrayPosition12 + 2);
                if (!source.cells[startArrayIndex12].samples || source.cells[startArrayIndex12].samples.length != 3)
                    throw new ArgumentError("samples must contain 3 elements");
                for (var startArrayIndex13:uint = 0; startArrayIndex13 < 3; startArrayIndex13++)
                {
                    const startArrayPosition13:uint = startArrayPosition12 + 4 + startArrayIndex13 * 4;
                    sf32(source.cells[startArrayIndex12].samples[startArrayIndex13], startArrayPosition13);
                }
            }
            si8(source.tail, start + 176);
            sf64(0, start + 177);
            si32(0, start + 185);
            si16(0, start + 189);
            si8(0, start + 191);
            return start;
        }

        /** Decode raw struct bytes at offset into a new or reused owned value. */
        public static function unpack(bytes:flash.utils.ByteArray, destination:Arrays = null, offset:uint = 0):Arrays
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.begin(context, bytes);
            try
            {
                destination = unpackFrom(context, offset, destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }

        /** Internal generated entry point; context must already own domain memory. */
        public static function unpackFrom(context:as3flatbuffers.UnpackContext, base:uint, destination:Arrays):Arrays
        {
            as3flatbuffers.Unpack.struct(context, base, 192);
            if (!destination)
                destination = new Arrays();

            for (var index0:uint = 0; index0 < 3; index0++)
            {
                const element0:uint = base + 0 + index0 * 1;
                destination.flags[index0] = (li8(element0) != 0);
            }

            for (var index1:uint = 0; index1 < 3; index1++)
            {
                const element1:uint = base + 3 + index1 * 1;
                destination.tiny[index1] = sxi8(li8(element1));
            }

            for (var index2:uint = 0; index2 < 3; index2++)
            {
                const element2:uint = base + 6 + index2 * 1;
                destination.octets[index2] = li8(element2);
            }

            for (var index3:uint = 0; index3 < 3; index3++)
            {
                const element3:uint = base + 10 + index3 * 2;
                destination.small[index3] = sxi16(li16(element3));
            }

            for (var index4:uint = 0; index4 < 3; index4++)
            {
                const element4:uint = base + 16 + index4 * 2;
                destination.shorts[index4] = li16(element4);
            }

            for (var index5:uint = 0; index5 < 3; index5++)
            {
                const element5:uint = base + 24 + index5 * 4;
                destination.ints[index5] = li32(element5);
            }

            for (var index6:uint = 0; index6 < 3; index6++)
            {
                const element6:uint = base + 36 + index6 * 4;
                destination.uints[index6] = uint(li32(element6));
            }

            for (var index7:uint = 0; index7 < 3; index7++)
            {
                const element7:uint = base + 48 + index7 * 8;
                if (!destination.longs[index7])
                    destination.longs[index7] = new as3flatbuffers.types.Int64();
                destination.longs[index7].set(uint(li32(element7)), li32(element7 + 4));
            }

            for (var index8:uint = 0; index8 < 3; index8++)
            {
                const element8:uint = base + 72 + index8 * 8;
                if (!destination.ulongs[index8])
                    destination.ulongs[index8] = new as3flatbuffers.types.UInt64();
                destination.ulongs[index8].set(uint(li32(element8)), uint(li32(element8 + 4)));
            }

            for (var index9:uint = 0; index9 < 3; index9++)
            {
                const element9:uint = base + 96 + index9 * 4;
                destination.floats[index9] = lf32(element9);
            }

            for (var index10:uint = 0; index10 < 3; index10++)
            {
                const element10:uint = base + 112 + index10 * 8;
                destination.doubles[index10] = lf64(element10);
            }

            for (var index11:uint = 0; index11 < 3; index11++)
            {
                const element11:uint = base + 136 + index11 * 1;
                destination.modes[index11] = li8(element11);
            }

            for (var index12:uint = 0; index12 < 2; index12++)
            {
                const element12:uint = base + 144 + index12 * 16;
                destination.cells[index12] = fixtures.arrays.Cell.unpackFrom(context, element12, destination.cells[index12]);
            }

            destination.tail = li8(base + 176);
            return destination;
        }
    }
}
