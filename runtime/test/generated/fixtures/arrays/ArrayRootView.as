// Code generated by as3flatc. DO NOT EDIT.
package fixtures.arrays
{
    import as3flatbuffers.TableView;
    import flash.utils.ByteArray;
    import fixtures.arrays.Arrays;
    import fixtures.arrays.ArraysView;

    /** Borrowed read-only view. Use the owned class unpack() API for independent values. */
    public final class ArrayRootView extends as3flatbuffers.TableView
    {
        private const valueView:fixtures.arrays.ArraysView = new fixtures.arrays.ArraysView();

        private const valuesView:fixtures.arrays.ArraysView = new fixtures.arrays.ArraysView();

        private const otherView:fixtures.arrays.ArraysView = new fixtures.arrays.ArraysView();

        public function get value():fixtures.arrays.ArraysView
        {
            const position:uint = fieldOffset(4, 192);
            return position ? this.valueView.bind(bytes, position) : null;
        }

        public function get valuesLength():uint
        {
            const position:uint = vectorOffset(6, 192);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function values(index:uint):fixtures.arrays.ArraysView
        {
            const vector:uint = vectorOffset(6, 192);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 192;
            return this.valuesView.bind(bytes, position);
        }

        public function get otherLength():uint
        {
            const position:uint = vectorOffset(8, 192);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function other(index:uint):fixtures.arrays.ArraysView
        {
            const vector:uint = vectorOffset(8, 192);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 192;
            return this.otherView.bind(bytes, position);
        }
    }
}
