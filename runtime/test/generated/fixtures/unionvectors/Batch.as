// Code generated by as3flatc. DO NOT EDIT.
package fixtures.unionvectors
{
    import as3flatbuffers.Pack;
    import as3flatbuffers.Unpack;
    import as3flatbuffers.UnpackContext;
    import avm2.intrinsics.memory.*;
    import flash.utils.ByteArray;
    import fixtures.unionvectors.Item;

    /** Owned mutable value. pack() writes a complete FlatBuffer into the destination. */
    public final class Batch
    {
        private static const PACK:as3flatbuffers.PackContext = new as3flatbuffers.PackContext();
        private static const UNPACK:as3flatbuffers.UnpackContext = new as3flatbuffers.UnpackContext();

        public var id:uint = 0;
        public var items:Vector.<fixtures.unionvectors.Item> = new Vector.<fixtures.unionvectors.Item>();
        public var mirror:Vector.<fixtures.unionvectors.Item> = new Vector.<fixtures.unionvectors.Item>();
        public var single:fixtures.unionvectors.Item = new fixtures.unionvectors.Item();
        public var tail:Boolean = false;

        public static function reset(msg:Batch):void
        {
            msg.id = 0;
            msg.items.length = 0;
            msg.mirror.length = 0;
            fixtures.unionvectors.Item.reset(msg.single);
            msg.tail = false;
        }

        public static function clone(source:Batch):Batch
        {
            if (!source)
                return null;

            const destination:Batch = new Batch();
            destination.id = source.id;
            destination.items.length = source.items.length;
            for (var index2:uint = 0; index2 < source.items.length; index2++)
            {
                destination.items[index2] = fixtures.unionvectors.Item.clone(source.items[index2]);
            }
            destination.mirror.length = source.mirror.length;
            for (var index4:uint = 0; index4 < source.mirror.length; index4++)
            {
                destination.mirror[index4] = fixtures.unionvectors.Item.clone(source.mirror[index4]);
            }
            destination.single = fixtures.unionvectors.Item.clone(source.single);
            destination.tail = source.tail;

            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function pack(source:Batch, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.begin(context, dst, true);
                as3flatbuffers.Pack.finish(context, packInto(source, context));
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Write into an active context and return the absolute object offset. */
        public static function packInto(source:Batch, context:as3flatbuffers.PackContext):uint
        {
            if (!source || !context)
                throw new ArgumentError("Source and context must be non-null");

            as3flatbuffers.Pack.ensure(context, 66);
            as3flatbuffers.Pack.prepare(context, 2);
            as3flatbuffers.Pack.reserveVtable(context, 8);
            as3flatbuffers.Pack.prepare(context, 4);
            as3flatbuffers.Pack.startTable(context);
            if (source.id != 0)
            {
                as3flatbuffers.Pack.addUint32(context, 0, source.id);
            }
            var tagsOffset2:uint = 0;
            var offset2:uint = 0;
            if (source.items.length)
            {
                tagsOffset2 = as3flatbuffers.Pack.reserveOffset(context, 1);
                offset2 = as3flatbuffers.Pack.reserveOffset(context, 2);
            }
            var tagsOffset4:uint = 0;
            var offset4:uint = 0;
            if (source.mirror.length)
            {
                tagsOffset4 = as3flatbuffers.Pack.reserveOffset(context, 3);
                offset4 = as3flatbuffers.Pack.reserveOffset(context, 4);
            }
            fixtures.unionvectors.Item.validate(source.single);
            var offset6:uint = 0;
            if (source.single.type)
            {
                as3flatbuffers.Pack.addUint8(context, 5, source.single.type);
                as3flatbuffers.Pack.prepare(context, 4);
                offset6 = as3flatbuffers.Pack.reserveOffset(context, 6);
            }
            if (source.tail != false)
            {
                as3flatbuffers.Pack.addBool(context, 7, source.tail);
            }
            const table:uint = as3flatbuffers.Pack.endTable(context);
            if (offset2)
            {
                const count2:uint = source.items.length;
                as3flatbuffers.Pack.prepareVector(context, 1, count2, 1);
                const tags2:uint = as3flatbuffers.Pack.startVector(context, count2);
                as3flatbuffers.Pack.patchOffset(context, tagsOffset2, tags2);
                const tagData2:uint = as3flatbuffers.Pack.reserve(context, count2);
                for (var tagIndex2:uint = 0; tagIndex2 < count2; tagIndex2++)
                {
                    fixtures.unionvectors.Item.validate(source.items[tagIndex2]);
                    si8(source.items[tagIndex2].type, tagData2 + tagIndex2);
                }
                as3flatbuffers.Pack.prepareVector(context, 4, count2, 4);
                const vector2:uint = as3flatbuffers.Pack.startVector(context, count2);
                as3flatbuffers.Pack.patchOffset(context, offset2, vector2);
                const data2:uint = as3flatbuffers.Pack.reserve(context, count2 * 4);
                for (var index2:uint = 0; index2 < count2; index2++)
                {
                    const reference2:uint = data2 + index2 * 4;
                    si32(0, reference2);
                    if (source.items[index2].type)
                        fixtures.unionvectors.Item.packInto(source.items[index2], context, reference2);
                }
            }
            if (offset4)
            {
                const count4:uint = source.mirror.length;
                as3flatbuffers.Pack.prepareVector(context, 1, count4, 1);
                const tags4:uint = as3flatbuffers.Pack.startVector(context, count4);
                as3flatbuffers.Pack.patchOffset(context, tagsOffset4, tags4);
                const tagData4:uint = as3flatbuffers.Pack.reserve(context, count4);
                for (var tagIndex4:uint = 0; tagIndex4 < count4; tagIndex4++)
                {
                    fixtures.unionvectors.Item.validate(source.mirror[tagIndex4]);
                    si8(source.mirror[tagIndex4].type, tagData4 + tagIndex4);
                }
                as3flatbuffers.Pack.prepareVector(context, 4, count4, 4);
                const vector4:uint = as3flatbuffers.Pack.startVector(context, count4);
                as3flatbuffers.Pack.patchOffset(context, offset4, vector4);
                const data4:uint = as3flatbuffers.Pack.reserve(context, count4 * 4);
                for (var index4:uint = 0; index4 < count4; index4++)
                {
                    const reference4:uint = data4 + index4 * 4;
                    si32(0, reference4);
                    if (source.mirror[index4].type)
                        fixtures.unionvectors.Item.packInto(source.mirror[index4], context, reference4);
                }
            }
            if (offset6)
            {
                fixtures.unionvectors.Item.packInto(source.single, context, offset6);
            }
            return table;
        }

        /** Decode a FlatBuffer whose root-offset word starts at offset. */
        public static function unpack(bytes:flash.utils.ByteArray, destination:Batch = null, offset:uint = 0):Batch
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.begin(context, bytes);
            try
            {
                destination = unpackFrom(context, as3flatbuffers.Unpack.root(context, offset), destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }

        /** Internal generated entry point; context must already own domain memory. */
        public static function unpackFrom(context:as3flatbuffers.UnpackContext, base:uint, destination:Batch):Batch
        {
            const vtable:uint = as3flatbuffers.Unpack.vtable(context, base);
            const vtableSize:uint = li16(vtable);
            const objectSize:uint = li16(vtable + 2);
            if (!destination)
                destination = new Batch();

            const position0:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 4, 4);
            if (!position0)
            {
                destination.id = 0;
            }
            else
            {
                destination.id = uint(li32(position0));
            }

            const tagsField2:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 6, 4);
            const field2:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 8, 4);
            const tags2:uint = as3flatbuffers.Unpack.vector(context, tagsField2, 1);
            const vector2:uint = as3flatbuffers.Unpack.vector(context, field2, 4);
            const tagCount2:uint = tags2 ? uint(li32(tags2)) : 0;
            const count2:uint = vector2 ? uint(li32(vector2)) : 0;
            if ((tags2 == 0) != (vector2 == 0) || tagCount2 != count2)
                throw new RangeError("Union tag and payload vectors must match");
            destination.items.length = count2;
            for (var index2:uint = 0; index2 < count2; index2++)
            {
                destination.items[index2] = fixtures.unionvectors.Item.unpackFrom(context, li8(tags2 + 4 + index2), vector2 + 4 + index2 * 4, destination.items[index2]);
            }

            const tagsField4:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 10, 4);
            const field4:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 12, 4);
            const tags4:uint = as3flatbuffers.Unpack.vector(context, tagsField4, 1);
            const vector4:uint = as3flatbuffers.Unpack.vector(context, field4, 4);
            const tagCount4:uint = tags4 ? uint(li32(tags4)) : 0;
            const count4:uint = vector4 ? uint(li32(vector4)) : 0;
            if ((tags4 == 0) != (vector4 == 0) || tagCount4 != count4)
                throw new RangeError("Union tag and payload vectors must match");
            destination.mirror.length = count4;
            for (var index4:uint = 0; index4 < count4; index4++)
            {
                destination.mirror[index4] = fixtures.unionvectors.Item.unpackFrom(context, li8(tags4 + 4 + index4), vector4 + 4 + index4 * 4, destination.mirror[index4]);
            }

            const tag6:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 14, 1);
            const field6:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 16, 4);
            destination.single = fixtures.unionvectors.Item.unpackFrom(context, tag6 ? li8(tag6) : 0, field6, destination.single);

            const position7:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 18, 1);
            if (!position7)
            {
                destination.tail = false;
            }
            else
            {
                destination.tail = (li8(position7) != 0);
            }
            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function packSizePrefixed(source:Batch, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.beginRoot(context, dst, true, false, 0);
                const root:uint = packInto(source, context);
                as3flatbuffers.Pack.finishRoot(context, root, true);
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Decode one size-prefixed frame at offset; references are bounded to its declared length. */
        public static function unpackSizePrefixed(bytes:flash.utils.ByteArray, destination:Batch = null, offset:uint = 0):Batch
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.beginSizePrefixed(context, bytes, offset);
            try
            {
                destination = unpackFrom(context, as3flatbuffers.Unpack.root(context, 4), destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }
    }
}
