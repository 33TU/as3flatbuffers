// Code generated by as3flatc. DO NOT EDIT.
package fixtures.keys
{
    import as3flatbuffers.types.Int64;
    import as3flatbuffers.types.UInt64;
    import as3flatbuffers.Keys;
    import as3flatbuffers.TableView;
    import flash.utils.ByteArray;
    import fixtures.keys.BoolKey;
    import fixtures.keys.BoolKeyView;
    import fixtures.keys.ByteKey;
    import fixtures.keys.ByteKeyView;
    import fixtures.keys.DoubleKey;
    import fixtures.keys.DoubleKeyView;
    import fixtures.keys.EnumKey;
    import fixtures.keys.EnumKeyView;
    import fixtures.keys.FloatKey;
    import fixtures.keys.FloatKeyView;
    import fixtures.keys.InlineKey;
    import fixtures.keys.InlineKeyView;
    import fixtures.keys.InlineLong;
    import fixtures.keys.InlineLongView;
    import fixtures.keys.LongKey;
    import fixtures.keys.LongKeyView;
    import fixtures.keys.ShortKey;
    import fixtures.keys.ShortKeyView;
    import fixtures.keys.Signed;
    import fixtures.keys.SignedView;
    import fixtures.keys.TextKey;
    import fixtures.keys.TextKeyView;
    import fixtures.keys.UByteKey;
    import fixtures.keys.UByteKeyView;
    import fixtures.keys.ULongKey;
    import fixtures.keys.ULongKeyView;
    import fixtures.keys.UShortKey;
    import fixtures.keys.UShortKeyView;
    import fixtures.keys.Unsigned;
    import fixtures.keys.UnsignedView;

    /** Borrowed read-only view; unpack() produces independent owned values. */
    public final class DirectoryView extends as3flatbuffers.TableView
    {
        private var signedView:fixtures.keys.SignedView;

        private var unsignedView:fixtures.keys.UnsignedView;

        private var textsView:fixtures.keys.TextKeyView;

        private var longsView:fixtures.keys.LongKeyView;

        private var ulongsView:fixtures.keys.ULongKeyView;

        private var floatsView:fixtures.keys.FloatKeyView;

        private var doublesView:fixtures.keys.DoubleKeyView;

        private var bytes_View:fixtures.keys.ByteKeyView;

        private var ubytesView:fixtures.keys.UByteKeyView;

        private var shortsView:fixtures.keys.ShortKeyView;

        private var ushortsView:fixtures.keys.UShortKeyView;

        private var boolsView:fixtures.keys.BoolKeyView;

        private var enumsView:fixtures.keys.EnumKeyView;

        private const inlineKeysView:fixtures.keys.InlineKeyView = new fixtures.keys.InlineKeyView();

        private const inlineLongsView:fixtures.keys.InlineLongView = new fixtures.keys.InlineLongView();

        public function get signedLength():uint
        {
            const position:uint = vectorOffset(4, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function signed(index:uint):fixtures.keys.SignedView
        {
            const vector:uint = vectorOffset(4, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            if (!this.signedView)
                this.signedView = new fixtures.keys.SignedView();

            const child:uint = referenceAt(position);
            this.signedView.bind(bytes, child);
            return this.signedView;
        }

        /** Binary search of an ascending key-sorted vector. Returns the same cached view as indexed access, or null. */
        public function signedByKey_2(key:int):fixtures.keys.SignedView
        {
            const vector:uint = vectorOffset(4, 4);
            if (!vector)
                return null;
            if (!this.signedView)
                this.signedView = new fixtures.keys.SignedView();

            bytes.position = vector;
            var span:uint = bytes.readUnsignedInt();
            var start:uint = 0;
            while (span)
            {
                const middle:uint = span >>> 1;
                const position:uint = vector + 4 + (start + middle) * 4;
                const child:uint = referenceAt(position);
                this.signedView.bind(bytes, child);
                const comparison:int = this.signedView.compareKey(key);
                if (comparison < 0)
                {
                    start += middle + 1;
                    span -= middle + 1;
                }
                else if (comparison > 0)
                    span = middle;
                else
                    return this.signedView;
            }
            return null;
        }

        public function get unsignedLength():uint
        {
            const position:uint = vectorOffset(6, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function unsigned(index:uint):fixtures.keys.UnsignedView
        {
            const vector:uint = vectorOffset(6, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            if (!this.unsignedView)
                this.unsignedView = new fixtures.keys.UnsignedView();

            const child:uint = referenceAt(position);
            this.unsignedView.bind(bytes, child);
            return this.unsignedView;
        }

        /** Binary search of an ascending key-sorted vector. Returns the same cached view as indexed access, or null. */
        public function unsignedByKey(key:uint):fixtures.keys.UnsignedView
        {
            const vector:uint = vectorOffset(6, 4);
            if (!vector)
                return null;
            if (!this.unsignedView)
                this.unsignedView = new fixtures.keys.UnsignedView();

            bytes.position = vector;
            var span:uint = bytes.readUnsignedInt();
            var start:uint = 0;
            while (span)
            {
                const middle:uint = span >>> 1;
                const position:uint = vector + 4 + (start + middle) * 4;
                const child:uint = referenceAt(position);
                this.unsignedView.bind(bytes, child);
                const comparison:int = this.unsignedView.compareKey(key);
                if (comparison < 0)
                {
                    start += middle + 1;
                    span -= middle + 1;
                }
                else if (comparison > 0)
                    span = middle;
                else
                    return this.unsignedView;
            }
            return null;
        }

        public function get textsLength():uint
        {
            const position:uint = vectorOffset(8, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function texts(index:uint):fixtures.keys.TextKeyView
        {
            const vector:uint = vectorOffset(8, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            if (!this.textsView)
                this.textsView = new fixtures.keys.TextKeyView();

            const child:uint = referenceAt(position);
            this.textsView.bind(bytes, child);
            return this.textsView;
        }

        /** Binary search of an ascending key-sorted vector. Returns the same cached view as indexed access, or null. */
        public function textsByKey(key:String):fixtures.keys.TextKeyView
        {
            if (key == null)
                throw new ArgumentError("Lookup key must be non-null");
            const vector:uint = vectorOffset(8, 4);
            if (!vector)
                return null;
            const encoded:flash.utils.ByteArray = new flash.utils.ByteArray();
            encoded.writeUTFBytes(key);
            if (!this.textsView)
                this.textsView = new fixtures.keys.TextKeyView();

            bytes.position = vector;
            var span:uint = bytes.readUnsignedInt();
            var start:uint = 0;
            while (span)
            {
                const middle:uint = span >>> 1;
                const position:uint = vector + 4 + (start + middle) * 4;
                const child:uint = referenceAt(position);
                this.textsView.bind(bytes, child);
                const comparison:int = this.textsView.compareKey(encoded);
                if (comparison < 0)
                {
                    start += middle + 1;
                    span -= middle + 1;
                }
                else if (comparison > 0)
                    span = middle;
                else
                    return this.textsView;
            }
            return null;
        }

        public function get longsLength():uint
        {
            const position:uint = vectorOffset(10, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function longs(index:uint):fixtures.keys.LongKeyView
        {
            const vector:uint = vectorOffset(10, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            if (!this.longsView)
                this.longsView = new fixtures.keys.LongKeyView();

            const child:uint = referenceAt(position);
            this.longsView.bind(bytes, child);
            return this.longsView;
        }

        /** Binary search of an ascending key-sorted vector. Returns the same cached view as indexed access, or null. */
        public function longsByKey(key:as3flatbuffers.types.Int64):fixtures.keys.LongKeyView
        {
            if (key == null)
                throw new ArgumentError("Lookup key must be non-null");
            const vector:uint = vectorOffset(10, 4);
            if (!vector)
                return null;
            if (!this.longsView)
                this.longsView = new fixtures.keys.LongKeyView();

            bytes.position = vector;
            var span:uint = bytes.readUnsignedInt();
            var start:uint = 0;
            while (span)
            {
                const middle:uint = span >>> 1;
                const position:uint = vector + 4 + (start + middle) * 4;
                const child:uint = referenceAt(position);
                this.longsView.bind(bytes, child);
                const comparison:int = this.longsView.compareKey(key);
                if (comparison < 0)
                {
                    start += middle + 1;
                    span -= middle + 1;
                }
                else if (comparison > 0)
                    span = middle;
                else
                    return this.longsView;
            }
            return null;
        }

        public function get ulongsLength():uint
        {
            const position:uint = vectorOffset(12, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function ulongs(index:uint):fixtures.keys.ULongKeyView
        {
            const vector:uint = vectorOffset(12, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            if (!this.ulongsView)
                this.ulongsView = new fixtures.keys.ULongKeyView();

            const child:uint = referenceAt(position);
            this.ulongsView.bind(bytes, child);
            return this.ulongsView;
        }

        /** Binary search of an ascending key-sorted vector. Returns the same cached view as indexed access, or null. */
        public function ulongsByKey(key:as3flatbuffers.types.UInt64):fixtures.keys.ULongKeyView
        {
            if (key == null)
                throw new ArgumentError("Lookup key must be non-null");
            const vector:uint = vectorOffset(12, 4);
            if (!vector)
                return null;
            if (!this.ulongsView)
                this.ulongsView = new fixtures.keys.ULongKeyView();

            bytes.position = vector;
            var span:uint = bytes.readUnsignedInt();
            var start:uint = 0;
            while (span)
            {
                const middle:uint = span >>> 1;
                const position:uint = vector + 4 + (start + middle) * 4;
                const child:uint = referenceAt(position);
                this.ulongsView.bind(bytes, child);
                const comparison:int = this.ulongsView.compareKey(key);
                if (comparison < 0)
                {
                    start += middle + 1;
                    span -= middle + 1;
                }
                else if (comparison > 0)
                    span = middle;
                else
                    return this.ulongsView;
            }
            return null;
        }

        public function get floatsLength():uint
        {
            const position:uint = vectorOffset(14, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function floats(index:uint):fixtures.keys.FloatKeyView
        {
            const vector:uint = vectorOffset(14, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            if (!this.floatsView)
                this.floatsView = new fixtures.keys.FloatKeyView();

            const child:uint = referenceAt(position);
            this.floatsView.bind(bytes, child);
            return this.floatsView;
        }

        /** Binary search of an ascending key-sorted vector. Returns the same cached view as indexed access, or null. */
        public function floatsByKey(key:Number):fixtures.keys.FloatKeyView
        {
            key = as3flatbuffers.Keys.float32(key);
            if (isNaN(key))
                throw new ArgumentError("NaN cannot be a lookup key");
            const vector:uint = vectorOffset(14, 4);
            if (!vector)
                return null;
            if (!this.floatsView)
                this.floatsView = new fixtures.keys.FloatKeyView();

            bytes.position = vector;
            var span:uint = bytes.readUnsignedInt();
            var start:uint = 0;
            while (span)
            {
                const middle:uint = span >>> 1;
                const position:uint = vector + 4 + (start + middle) * 4;
                const child:uint = referenceAt(position);
                this.floatsView.bind(bytes, child);
                const comparison:int = this.floatsView.compareKey(key);
                if (comparison < 0)
                {
                    start += middle + 1;
                    span -= middle + 1;
                }
                else if (comparison > 0)
                    span = middle;
                else
                    return this.floatsView;
            }
            return null;
        }

        public function get doublesLength():uint
        {
            const position:uint = vectorOffset(16, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function doubles(index:uint):fixtures.keys.DoubleKeyView
        {
            const vector:uint = vectorOffset(16, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            if (!this.doublesView)
                this.doublesView = new fixtures.keys.DoubleKeyView();

            const child:uint = referenceAt(position);
            this.doublesView.bind(bytes, child);
            return this.doublesView;
        }

        /** Binary search of an ascending key-sorted vector. Returns the same cached view as indexed access, or null. */
        public function doublesByKey(key:Number):fixtures.keys.DoubleKeyView
        {
            if (isNaN(key))
                throw new ArgumentError("NaN cannot be a lookup key");
            const vector:uint = vectorOffset(16, 4);
            if (!vector)
                return null;
            if (!this.doublesView)
                this.doublesView = new fixtures.keys.DoubleKeyView();

            bytes.position = vector;
            var span:uint = bytes.readUnsignedInt();
            var start:uint = 0;
            while (span)
            {
                const middle:uint = span >>> 1;
                const position:uint = vector + 4 + (start + middle) * 4;
                const child:uint = referenceAt(position);
                this.doublesView.bind(bytes, child);
                const comparison:int = this.doublesView.compareKey(key);
                if (comparison < 0)
                {
                    start += middle + 1;
                    span -= middle + 1;
                }
                else if (comparison > 0)
                    span = middle;
                else
                    return this.doublesView;
            }
            return null;
        }

        public function get bytes_Length():uint
        {
            const position:uint = vectorOffset(18, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function bytes_(index:uint):fixtures.keys.ByteKeyView
        {
            const vector:uint = vectorOffset(18, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            if (!this.bytes_View)
                this.bytes_View = new fixtures.keys.ByteKeyView();

            const child:uint = referenceAt(position);
            this.bytes_View.bind(bytes, child);
            return this.bytes_View;
        }

        /** Binary search of an ascending key-sorted vector. Returns the same cached view as indexed access, or null. */
        public function bytes_ByKey(key:int):fixtures.keys.ByteKeyView
        {
            key = (key << 24 >> 24);
            const vector:uint = vectorOffset(18, 4);
            if (!vector)
                return null;
            if (!this.bytes_View)
                this.bytes_View = new fixtures.keys.ByteKeyView();

            bytes.position = vector;
            var span:uint = bytes.readUnsignedInt();
            var start:uint = 0;
            while (span)
            {
                const middle:uint = span >>> 1;
                const position:uint = vector + 4 + (start + middle) * 4;
                const child:uint = referenceAt(position);
                this.bytes_View.bind(bytes, child);
                const comparison:int = this.bytes_View.compareKey(key);
                if (comparison < 0)
                {
                    start += middle + 1;
                    span -= middle + 1;
                }
                else if (comparison > 0)
                    span = middle;
                else
                    return this.bytes_View;
            }
            return null;
        }

        public function get ubytesLength():uint
        {
            const position:uint = vectorOffset(20, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function ubytes(index:uint):fixtures.keys.UByteKeyView
        {
            const vector:uint = vectorOffset(20, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            if (!this.ubytesView)
                this.ubytesView = new fixtures.keys.UByteKeyView();

            const child:uint = referenceAt(position);
            this.ubytesView.bind(bytes, child);
            return this.ubytesView;
        }

        /** Binary search of an ascending key-sorted vector. Returns the same cached view as indexed access, or null. */
        public function ubytesByKey(key:uint):fixtures.keys.UByteKeyView
        {
            key = (key & 255);
            const vector:uint = vectorOffset(20, 4);
            if (!vector)
                return null;
            if (!this.ubytesView)
                this.ubytesView = new fixtures.keys.UByteKeyView();

            bytes.position = vector;
            var span:uint = bytes.readUnsignedInt();
            var start:uint = 0;
            while (span)
            {
                const middle:uint = span >>> 1;
                const position:uint = vector + 4 + (start + middle) * 4;
                const child:uint = referenceAt(position);
                this.ubytesView.bind(bytes, child);
                const comparison:int = this.ubytesView.compareKey(key);
                if (comparison < 0)
                {
                    start += middle + 1;
                    span -= middle + 1;
                }
                else if (comparison > 0)
                    span = middle;
                else
                    return this.ubytesView;
            }
            return null;
        }

        public function get shortsLength():uint
        {
            const position:uint = vectorOffset(22, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function shorts(index:uint):fixtures.keys.ShortKeyView
        {
            const vector:uint = vectorOffset(22, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            if (!this.shortsView)
                this.shortsView = new fixtures.keys.ShortKeyView();

            const child:uint = referenceAt(position);
            this.shortsView.bind(bytes, child);
            return this.shortsView;
        }

        /** Binary search of an ascending key-sorted vector. Returns the same cached view as indexed access, or null. */
        public function shortsByKey(key:int):fixtures.keys.ShortKeyView
        {
            key = (key << 16 >> 16);
            const vector:uint = vectorOffset(22, 4);
            if (!vector)
                return null;
            if (!this.shortsView)
                this.shortsView = new fixtures.keys.ShortKeyView();

            bytes.position = vector;
            var span:uint = bytes.readUnsignedInt();
            var start:uint = 0;
            while (span)
            {
                const middle:uint = span >>> 1;
                const position:uint = vector + 4 + (start + middle) * 4;
                const child:uint = referenceAt(position);
                this.shortsView.bind(bytes, child);
                const comparison:int = this.shortsView.compareKey(key);
                if (comparison < 0)
                {
                    start += middle + 1;
                    span -= middle + 1;
                }
                else if (comparison > 0)
                    span = middle;
                else
                    return this.shortsView;
            }
            return null;
        }

        public function get ushortsLength():uint
        {
            const position:uint = vectorOffset(24, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function ushorts(index:uint):fixtures.keys.UShortKeyView
        {
            const vector:uint = vectorOffset(24, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            if (!this.ushortsView)
                this.ushortsView = new fixtures.keys.UShortKeyView();

            const child:uint = referenceAt(position);
            this.ushortsView.bind(bytes, child);
            return this.ushortsView;
        }

        /** Binary search of an ascending key-sorted vector. Returns the same cached view as indexed access, or null. */
        public function ushortsByKey(key:uint):fixtures.keys.UShortKeyView
        {
            key = (key & 65535);
            const vector:uint = vectorOffset(24, 4);
            if (!vector)
                return null;
            if (!this.ushortsView)
                this.ushortsView = new fixtures.keys.UShortKeyView();

            bytes.position = vector;
            var span:uint = bytes.readUnsignedInt();
            var start:uint = 0;
            while (span)
            {
                const middle:uint = span >>> 1;
                const position:uint = vector + 4 + (start + middle) * 4;
                const child:uint = referenceAt(position);
                this.ushortsView.bind(bytes, child);
                const comparison:int = this.ushortsView.compareKey(key);
                if (comparison < 0)
                {
                    start += middle + 1;
                    span -= middle + 1;
                }
                else if (comparison > 0)
                    span = middle;
                else
                    return this.ushortsView;
            }
            return null;
        }

        public function get boolsLength():uint
        {
            const position:uint = vectorOffset(26, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function bools(index:uint):fixtures.keys.BoolKeyView
        {
            const vector:uint = vectorOffset(26, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            if (!this.boolsView)
                this.boolsView = new fixtures.keys.BoolKeyView();

            const child:uint = referenceAt(position);
            this.boolsView.bind(bytes, child);
            return this.boolsView;
        }

        /** Binary search of an ascending key-sorted vector. Returns the same cached view as indexed access, or null. */
        public function boolsByKey(key:Boolean):fixtures.keys.BoolKeyView
        {
            const vector:uint = vectorOffset(26, 4);
            if (!vector)
                return null;
            if (!this.boolsView)
                this.boolsView = new fixtures.keys.BoolKeyView();

            bytes.position = vector;
            var span:uint = bytes.readUnsignedInt();
            var start:uint = 0;
            while (span)
            {
                const middle:uint = span >>> 1;
                const position:uint = vector + 4 + (start + middle) * 4;
                const child:uint = referenceAt(position);
                this.boolsView.bind(bytes, child);
                const comparison:int = this.boolsView.compareKey(key);
                if (comparison < 0)
                {
                    start += middle + 1;
                    span -= middle + 1;
                }
                else if (comparison > 0)
                    span = middle;
                else
                    return this.boolsView;
            }
            return null;
        }

        public function get enumsLength():uint
        {
            const position:uint = vectorOffset(28, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function enums(index:uint):fixtures.keys.EnumKeyView
        {
            const vector:uint = vectorOffset(28, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            if (!this.enumsView)
                this.enumsView = new fixtures.keys.EnumKeyView();

            const child:uint = referenceAt(position);
            this.enumsView.bind(bytes, child);
            return this.enumsView;
        }

        /** Binary search of an ascending key-sorted vector. Returns the same cached view as indexed access, or null. */
        public function enumsByKey(key:int):fixtures.keys.EnumKeyView
        {
            key = (key << 16 >> 16);
            const vector:uint = vectorOffset(28, 4);
            if (!vector)
                return null;
            if (!this.enumsView)
                this.enumsView = new fixtures.keys.EnumKeyView();

            bytes.position = vector;
            var span:uint = bytes.readUnsignedInt();
            var start:uint = 0;
            while (span)
            {
                const middle:uint = span >>> 1;
                const position:uint = vector + 4 + (start + middle) * 4;
                const child:uint = referenceAt(position);
                this.enumsView.bind(bytes, child);
                const comparison:int = this.enumsView.compareKey(key);
                if (comparison < 0)
                {
                    start += middle + 1;
                    span -= middle + 1;
                }
                else if (comparison > 0)
                    span = middle;
                else
                    return this.enumsView;
            }
            return null;
        }

        public function get inlineKeysLength():uint
        {
            const position:uint = vectorOffset(30, 16);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function inlineKeys(index:uint):fixtures.keys.InlineKeyView
        {
            const vector:uint = vectorOffset(30, 16);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 16;
            return this.inlineKeysView.bind(bytes, position);
        }

        /** Binary search of an ascending key-sorted vector. Returns the same cached view as indexed access, or null. */
        public function inlineKeysByKey(key:int):fixtures.keys.InlineKeyView
        {
            const vector:uint = vectorOffset(30, 16);
            if (!vector)
                return null;
            bytes.position = vector;
            var span:uint = bytes.readUnsignedInt();
            var start:uint = 0;
            while (span)
            {
                const middle:uint = span >>> 1;
                const position:uint = vector + 4 + (start + middle) * 16;
                this.inlineKeysView.bind(bytes, position);
                const comparison:int = this.inlineKeysView.compareKey(key);
                if (comparison < 0)
                {
                    start += middle + 1;
                    span -= middle + 1;
                }
                else if (comparison > 0)
                    span = middle;
                else
                    return this.inlineKeysView;
            }
            return null;
        }

        public function get inlineLongsLength():uint
        {
            const position:uint = vectorOffset(32, 16);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function inlineLongs(index:uint):fixtures.keys.InlineLongView
        {
            const vector:uint = vectorOffset(32, 16);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 16;
            return this.inlineLongsView.bind(bytes, position);
        }

        /** Binary search of an ascending key-sorted vector. Returns the same cached view as indexed access, or null. */
        public function inlineLongsByKey(key:as3flatbuffers.types.Int64):fixtures.keys.InlineLongView
        {
            if (key == null)
                throw new ArgumentError("Lookup key must be non-null");
            const vector:uint = vectorOffset(32, 16);
            if (!vector)
                return null;
            bytes.position = vector;
            var span:uint = bytes.readUnsignedInt();
            var start:uint = 0;
            while (span)
            {
                const middle:uint = span >>> 1;
                const position:uint = vector + 4 + (start + middle) * 16;
                this.inlineLongsView.bind(bytes, position);
                const comparison:int = this.inlineLongsView.compareKey(key);
                if (comparison < 0)
                {
                    start += middle + 1;
                    span -= middle + 1;
                }
                else if (comparison > 0)
                    span = middle;
                else
                    return this.inlineLongsView;
            }
            return null;
        }

        public function get signedByKey():String
        {
            return stringValue(34);
        }

        public function get signedByKey_():String
        {
            return stringValue(36);
        }
    }
}
