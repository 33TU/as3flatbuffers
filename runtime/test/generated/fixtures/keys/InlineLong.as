// Code generated by as3flatc. DO NOT EDIT.
package fixtures.keys
{
    import as3flatbuffers.Pack;
    import as3flatbuffers.Unpack;
    import as3flatbuffers.UnpackContext;
    import avm2.intrinsics.memory.*;
    import flash.utils.ByteArray;
    import as3flatbuffers.types.Int64;

    /** Owned inline struct. pack() writes raw struct bytes into the destination. */
    public final class InlineLong
    {
        private static const PACK:as3flatbuffers.PackContext = new as3flatbuffers.PackContext();
        private static const UNPACK:as3flatbuffers.UnpackContext = new as3flatbuffers.UnpackContext();

        /** Sort this vector in place by serialized key order before packing. */
        public static function sortByKey(values:Vector.<InlineLong>):void
        {
            if (!values)
                throw new ArgumentError("Key vector must be non-null");
            for each (var value:InlineLong in values)
                compareKeys(value, value);
            values.sort(compareKeys);
        }

        /** Comparator for owned values; narrow scalars use their wire representation. */
        public static function compareKeys(left:InlineLong, right:InlineLong):Number
        {
            if (!left || !right)
                throw new ArgumentError("Key vector elements must be non-null");
            if (!left.id || !right.id)
                throw new ArgumentError("64-bit keys must be non-null");
            return left.id.cmp(right.id);
        }

        public var id:as3flatbuffers.types.Int64 = new as3flatbuffers.types.Int64(0, 0);
        public var value:uint = 0;

        public static function reset(msg:InlineLong):void
        {
            msg.id.reset();
            msg.value = 0;
        }

        public static function clone(source:InlineLong):InlineLong
        {
            if (!source)
                return null;

            const destination:InlineLong = new InlineLong();
            destination.id.copyFrom(source.id);
            destination.value = source.value;

            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function pack(source:InlineLong, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.begin(context, dst, false);
                as3flatbuffers.Pack.finish(context, packInto(source, context));
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Write into an active context and return the absolute object offset. */
        public static function packInto(source:InlineLong, context:as3flatbuffers.PackContext):uint
        {
            if (!source || !context)
                throw new ArgumentError("Source and context must be non-null");

            const start:uint = as3flatbuffers.Pack.prepareStruct(context, 16, 8);
            if (!source.id)
                throw new ArgumentError("source.id must be non-null");
            si32(source.id.low, start + 0);
            si32(source.id.high, start + 0 + 4);
            si32(source.value, start + 8);
            si32(0, start + 12);
            return start;
        }

        /** Decode raw struct bytes at offset into a new or reused owned value. */
        public static function unpack(bytes:flash.utils.ByteArray, destination:InlineLong = null, offset:uint = 0):InlineLong
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.begin(context, bytes);
            try
            {
                destination = unpackFrom(context, offset, destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }

        /** Internal generated entry point; context must already own domain memory. */
        public static function unpackFrom(context:as3flatbuffers.UnpackContext, base:uint, destination:InlineLong):InlineLong
        {
            as3flatbuffers.Unpack.struct(context, base, 16);
            if (!destination)
                destination = new InlineLong();

            if (!destination.id)
                destination.id = new as3flatbuffers.types.Int64();

            destination.id.set(uint(li32(base + 0)), li32(base + 4));

            destination.value = uint(li32(base + 8));
            return destination;
        }
    }
}
