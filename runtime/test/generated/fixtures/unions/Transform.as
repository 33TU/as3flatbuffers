// Code generated by as3flatc. DO NOT EDIT.
package fixtures.unions
{
    import as3flatbuffers.Pack;
    import as3flatbuffers.Unpack;
    import as3flatbuffers.UnpackContext;
    import avm2.intrinsics.memory.*;
    import flash.utils.ByteArray;

    /** Owned inline struct. pack() writes raw struct bytes into the destination. */
    public final class Transform
    {
        private static const PACK:as3flatbuffers.PackContext = new as3flatbuffers.PackContext();
        private static const UNPACK:as3flatbuffers.UnpackContext = new as3flatbuffers.UnpackContext();

        public var matrix:Vector.<Number> = new Vector.<Number>(4, true);

        public function Transform()
        {
            for (var index0:uint = 0; index0 < 4; index0++)
            {
                this.matrix[index0] = 0;
            }
        }

        public static function reset(msg:Transform):void
        {
            for (var index0:uint = 0; index0 < 4; index0++)
            {
                msg.matrix[index0] = 0;
            }
        }

        public static function clone(source:Transform):Transform
        {
            if (!source)
                return null;

            const destination:Transform = new Transform();
            for (var index0:uint = 0; index0 < 4; index0++)
            {
                destination.matrix[index0] = source.matrix[index0];
            }

            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function pack(source:Transform, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.begin(context, dst, false);
                as3flatbuffers.Pack.finish(context, packInto(source, context));
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Write into an active context and return the absolute object offset. */
        public static function packInto(source:Transform, context:as3flatbuffers.PackContext):uint
        {
            if (!source || !context)
                throw new ArgumentError("Source and context must be non-null");

            const start:uint = as3flatbuffers.Pack.prepareStruct(context, 16, 4);
            if (!source.matrix || source.matrix.length != 4)
                throw new ArgumentError("matrix must contain 4 elements");
            for (var startArrayIndex0:uint = 0; startArrayIndex0 < 4; startArrayIndex0++)
            {
                const startArrayPosition0:uint = start + 0 + startArrayIndex0 * 4;
                sf32(source.matrix[startArrayIndex0], startArrayPosition0);
            }
            return start;
        }

        /** Decode raw struct bytes at offset into a new or reused owned value. */
        public static function unpack(bytes:flash.utils.ByteArray, destination:Transform = null, offset:uint = 0):Transform
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.begin(context, bytes);
            try
            {
                destination = unpackFrom(context, offset, destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }

        /** Internal generated entry point; context must already own domain memory. */
        public static function unpackFrom(context:as3flatbuffers.UnpackContext, base:uint, destination:Transform):Transform
        {
            as3flatbuffers.Unpack.struct(context, base, 16);
            if (!destination)
                destination = new Transform();

            for (var index0:uint = 0; index0 < 4; index0++)
            {
                const element0:uint = base + 0 + index0 * 4;
                destination.matrix[index0] = lf32(element0);
            }
            return destination;
        }
    }
}
