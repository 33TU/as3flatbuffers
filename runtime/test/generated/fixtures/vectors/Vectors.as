// Code generated by as3flatc. DO NOT EDIT.
package fixtures.vectors
{
    import as3flatbuffers.Pack;
    import as3flatbuffers.Unpack;
    import as3flatbuffers.UnpackContext;
    import avm2.intrinsics.memory.*;
    import flash.utils.ByteArray;
    import as3flatbuffers.types.Int64;
    import as3flatbuffers.types.UInt64;
    import fixtures.vectors.Aligned;
    import fixtures.vectors.Entry;
    import fixtures.vectors.Pair;

    /** Owned mutable value. pack() writes a complete FlatBuffer into the destination. */
    public final class Vectors
    {
        private static const PACK:as3flatbuffers.PackContext = new as3flatbuffers.PackContext();
        private static const UNPACK:as3flatbuffers.UnpackContext = new as3flatbuffers.UnpackContext();

        public var flags:Vector.<Boolean> = new Vector.<Boolean>();
        public var signedBytes:Vector.<int> = new Vector.<int>();
        public var unsignedBytes:Vector.<uint> = new Vector.<uint>();
        public var signedShorts:Vector.<int> = new Vector.<int>();
        public var unsignedShorts:Vector.<uint> = new Vector.<uint>();
        public var ints:Vector.<int> = new Vector.<int>();
        public var uints:Vector.<uint> = new Vector.<uint>();
        public var longs:Vector.<as3flatbuffers.types.Int64> = new Vector.<as3flatbuffers.types.Int64>();
        public var ulongs:Vector.<as3flatbuffers.types.UInt64> = new Vector.<as3flatbuffers.types.UInt64>();
        public var floats:Vector.<Number> = new Vector.<Number>();
        public var doubles:Vector.<Number> = new Vector.<Number>();
        public var texts:Vector.<String> = new Vector.<String>();
        public var points:Vector.<fixtures.vectors.Pair> = new Vector.<fixtures.vectors.Pair>();
        public var aligned:Vector.<fixtures.vectors.Aligned> = new Vector.<fixtures.vectors.Aligned>();
        public var entries:Vector.<fixtures.vectors.Entry> = new Vector.<fixtures.vectors.Entry>();
        public var intsPresent:int = 0;
        public var entriesView:int = 0;

        public static function reset(msg:Vectors):void
        {
            msg.flags.length = 0;
            msg.signedBytes.length = 0;
            msg.unsignedBytes.length = 0;
            msg.signedShorts.length = 0;
            msg.unsignedShorts.length = 0;
            msg.ints.length = 0;
            msg.uints.length = 0;
            msg.longs.length = 0;
            msg.ulongs.length = 0;
            msg.floats.length = 0;
            msg.doubles.length = 0;
            msg.texts.length = 0;
            msg.points.length = 0;
            msg.aligned.length = 0;
            msg.entries.length = 0;
            msg.intsPresent = 0;
            msg.entriesView = 0;
        }

        public static function clone(source:Vectors):Vectors
        {
            if (!source)
                return null;

            const destination:Vectors = new Vectors();
            destination.flags = source.flags.concat();
            destination.signedBytes = source.signedBytes.concat();
            destination.unsignedBytes = source.unsignedBytes.concat();
            destination.signedShorts = source.signedShorts.concat();
            destination.unsignedShorts = source.unsignedShorts.concat();
            destination.ints = source.ints.concat();
            destination.uints = source.uints.concat();
            destination.longs.length = source.longs.length;
            for (var index7:uint = 0; index7 < source.longs.length; index7++)
            {
                destination.longs[index7] = source.longs[index7] ? source.longs[index7].clone() : null;
            }
            destination.ulongs.length = source.ulongs.length;
            for (var index8:uint = 0; index8 < source.ulongs.length; index8++)
            {
                destination.ulongs[index8] = source.ulongs[index8] ? source.ulongs[index8].clone() : null;
            }
            destination.floats = source.floats.concat();
            destination.doubles = source.doubles.concat();
            destination.texts = source.texts.concat();
            destination.points.length = source.points.length;
            for (var index12:uint = 0; index12 < source.points.length; index12++)
            {
                destination.points[index12] = fixtures.vectors.Pair.clone(source.points[index12]);
            }
            destination.aligned.length = source.aligned.length;
            for (var index13:uint = 0; index13 < source.aligned.length; index13++)
            {
                destination.aligned[index13] = fixtures.vectors.Aligned.clone(source.aligned[index13]);
            }
            destination.entries.length = source.entries.length;
            for (var index14:uint = 0; index14 < source.entries.length; index14++)
            {
                destination.entries[index14] = fixtures.vectors.Entry.clone(source.entries[index14]);
            }
            destination.intsPresent = source.intsPresent;
            destination.entriesView = source.entriesView;

            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function pack(source:Vectors, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.begin(context, dst, true);
                as3flatbuffers.Pack.finish(context, packInto(source, context));
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Write into an active context and return the absolute object offset. */
        public static function packInto(source:Vectors, context:as3flatbuffers.PackContext):uint
        {
            if (!source || !context)
                throw new ArgumentError("Source and context must be non-null");

            as3flatbuffers.Pack.ensure(context, 165);
            as3flatbuffers.Pack.prepare(context, 2);
            as3flatbuffers.Pack.reserveVtable(context, 17);
            as3flatbuffers.Pack.prepare(context, 4);
            as3flatbuffers.Pack.startTable(context);
            const offset0:uint = source.flags.length ? as3flatbuffers.Pack.reserveOffset(context, 0) : 0;
            const offset1:uint = source.signedBytes.length ? as3flatbuffers.Pack.reserveOffset(context, 1) : 0;
            const offset2:uint = source.unsignedBytes.length ? as3flatbuffers.Pack.reserveOffset(context, 2) : 0;
            const offset3:uint = source.signedShorts.length ? as3flatbuffers.Pack.reserveOffset(context, 3) : 0;
            const offset4:uint = source.unsignedShorts.length ? as3flatbuffers.Pack.reserveOffset(context, 4) : 0;
            const offset5:uint = source.ints.length ? as3flatbuffers.Pack.reserveOffset(context, 5) : 0;
            const offset6:uint = source.uints.length ? as3flatbuffers.Pack.reserveOffset(context, 6) : 0;
            const offset7:uint = source.longs.length ? as3flatbuffers.Pack.reserveOffset(context, 7) : 0;
            const offset8:uint = source.ulongs.length ? as3flatbuffers.Pack.reserveOffset(context, 8) : 0;
            const offset9:uint = source.floats.length ? as3flatbuffers.Pack.reserveOffset(context, 9) : 0;
            const offset10:uint = source.doubles.length ? as3flatbuffers.Pack.reserveOffset(context, 10) : 0;
            const offset11:uint = source.texts.length ? as3flatbuffers.Pack.reserveOffset(context, 11) : 0;
            const offset12:uint = source.points.length ? as3flatbuffers.Pack.reserveOffset(context, 12) : 0;
            const offset13:uint = source.aligned.length ? as3flatbuffers.Pack.reserveOffset(context, 13) : 0;
            const offset14:uint = source.entries.length ? as3flatbuffers.Pack.reserveOffset(context, 14) : 0;
            if (source.intsPresent != 0)
            {
                as3flatbuffers.Pack.addInt32(context, 15, source.intsPresent);
            }
            if (source.entriesView != 0)
            {
                as3flatbuffers.Pack.addInt32(context, 16, source.entriesView);
            }
            const table:uint = as3flatbuffers.Pack.endTable(context);
            if (offset0)
            {
                as3flatbuffers.Pack.prepareVector(context, 1, source.flags.length, 1);
                const vector0:uint = as3flatbuffers.Pack.startVector(context, source.flags.length);
                as3flatbuffers.Pack.patchOffset(context, offset0, vector0);
                const data0:uint = as3flatbuffers.Pack.reserve(context, source.flags.length * 1);
                for (var index0:uint = 0; index0 < source.flags.length; index0++)
                {
                    si8((source.flags[index0] ? 1 : 0), data0 + index0 * 1);
                }
            }
            if (offset1)
            {
                as3flatbuffers.Pack.prepareVector(context, 1, source.signedBytes.length, 1);
                const vector1:uint = as3flatbuffers.Pack.startVector(context, source.signedBytes.length);
                as3flatbuffers.Pack.patchOffset(context, offset1, vector1);
                const data1:uint = as3flatbuffers.Pack.reserve(context, source.signedBytes.length * 1);
                for (var index1:uint = 0; index1 < source.signedBytes.length; index1++)
                {
                    si8(source.signedBytes[index1], data1 + index1 * 1);
                }
            }
            if (offset2)
            {
                as3flatbuffers.Pack.prepareVector(context, 1, source.unsignedBytes.length, 1);
                const vector2:uint = as3flatbuffers.Pack.startVector(context, source.unsignedBytes.length);
                as3flatbuffers.Pack.patchOffset(context, offset2, vector2);
                const data2:uint = as3flatbuffers.Pack.reserve(context, source.unsignedBytes.length * 1);
                for (var index2:uint = 0; index2 < source.unsignedBytes.length; index2++)
                {
                    si8(source.unsignedBytes[index2], data2 + index2 * 1);
                }
            }
            if (offset3)
            {
                as3flatbuffers.Pack.prepareVector(context, 2, source.signedShorts.length, 2);
                const vector3:uint = as3flatbuffers.Pack.startVector(context, source.signedShorts.length);
                as3flatbuffers.Pack.patchOffset(context, offset3, vector3);
                const data3:uint = as3flatbuffers.Pack.reserve(context, source.signedShorts.length * 2);
                for (var index3:uint = 0; index3 < source.signedShorts.length; index3++)
                {
                    si16(source.signedShorts[index3], data3 + index3 * 2);
                }
            }
            if (offset4)
            {
                as3flatbuffers.Pack.prepareVector(context, 2, source.unsignedShorts.length, 2);
                const vector4:uint = as3flatbuffers.Pack.startVector(context, source.unsignedShorts.length);
                as3flatbuffers.Pack.patchOffset(context, offset4, vector4);
                const data4:uint = as3flatbuffers.Pack.reserve(context, source.unsignedShorts.length * 2);
                for (var index4:uint = 0; index4 < source.unsignedShorts.length; index4++)
                {
                    si16(source.unsignedShorts[index4], data4 + index4 * 2);
                }
            }
            if (offset5)
            {
                as3flatbuffers.Pack.prepareVector(context, 4, source.ints.length, 4);
                const vector5:uint = as3flatbuffers.Pack.startVector(context, source.ints.length);
                as3flatbuffers.Pack.patchOffset(context, offset5, vector5);
                const data5:uint = as3flatbuffers.Pack.reserve(context, source.ints.length * 4);
                for (var index5:uint = 0; index5 < source.ints.length; index5++)
                {
                    si32(source.ints[index5], data5 + index5 * 4);
                }
            }
            if (offset6)
            {
                as3flatbuffers.Pack.prepareVector(context, 4, source.uints.length, 4);
                const vector6:uint = as3flatbuffers.Pack.startVector(context, source.uints.length);
                as3flatbuffers.Pack.patchOffset(context, offset6, vector6);
                const data6:uint = as3flatbuffers.Pack.reserve(context, source.uints.length * 4);
                for (var index6:uint = 0; index6 < source.uints.length; index6++)
                {
                    si32(source.uints[index6], data6 + index6 * 4);
                }
            }
            if (offset7)
            {
                as3flatbuffers.Pack.prepareVector(context, 8, source.longs.length, 8);
                const vector7:uint = as3flatbuffers.Pack.startVector(context, source.longs.length);
                as3flatbuffers.Pack.patchOffset(context, offset7, vector7);
                const data7:uint = as3flatbuffers.Pack.reserve(context, source.longs.length * 8);
                for (var index7:uint = 0; index7 < source.longs.length; index7++)
                {
                    if (source.longs[index7] == null)
                        throw new ArgumentError("longs elements must be non-null");
                    si32(source.longs[index7].low, data7 + index7 * 8);
                    si32(source.longs[index7].high, data7 + index7 * 8 + 4);
                }
            }
            if (offset8)
            {
                as3flatbuffers.Pack.prepareVector(context, 8, source.ulongs.length, 8);
                const vector8:uint = as3flatbuffers.Pack.startVector(context, source.ulongs.length);
                as3flatbuffers.Pack.patchOffset(context, offset8, vector8);
                const data8:uint = as3flatbuffers.Pack.reserve(context, source.ulongs.length * 8);
                for (var index8:uint = 0; index8 < source.ulongs.length; index8++)
                {
                    if (source.ulongs[index8] == null)
                        throw new ArgumentError("ulongs elements must be non-null");
                    si32(source.ulongs[index8].low, data8 + index8 * 8);
                    si32(source.ulongs[index8].high, data8 + index8 * 8 + 4);
                }
            }
            if (offset9)
            {
                as3flatbuffers.Pack.prepareVector(context, 4, source.floats.length, 4);
                const vector9:uint = as3flatbuffers.Pack.startVector(context, source.floats.length);
                as3flatbuffers.Pack.patchOffset(context, offset9, vector9);
                const data9:uint = as3flatbuffers.Pack.reserve(context, source.floats.length * 4);
                for (var index9:uint = 0; index9 < source.floats.length; index9++)
                {
                    sf32(source.floats[index9], data9 + index9 * 4);
                }
            }
            if (offset10)
            {
                as3flatbuffers.Pack.prepareVector(context, 8, source.doubles.length, 8);
                const vector10:uint = as3flatbuffers.Pack.startVector(context, source.doubles.length);
                as3flatbuffers.Pack.patchOffset(context, offset10, vector10);
                const data10:uint = as3flatbuffers.Pack.reserve(context, source.doubles.length * 8);
                for (var index10:uint = 0; index10 < source.doubles.length; index10++)
                {
                    sf64(source.doubles[index10], data10 + index10 * 8);
                }
            }
            if (offset11)
            {
                as3flatbuffers.Pack.prepareVector(context, 4, source.texts.length, 4);
                const vector11:uint = as3flatbuffers.Pack.startVector(context, source.texts.length);
                as3flatbuffers.Pack.patchOffset(context, offset11, vector11);
                const data11:uint = as3flatbuffers.Pack.reserve(context, source.texts.length * 4);
                for (var index11:uint = 0; index11 < source.texts.length; index11++)
                {
                    if (source.texts[index11] == null)
                        throw new ArgumentError("texts elements must be non-null");
                    as3flatbuffers.Pack.prepare(context, 4);
                    as3flatbuffers.Pack.writeString(context, data11 + index11 * 4, source.texts[index11]);
                }
            }
            if (offset12)
            {
                as3flatbuffers.Pack.prepareVector(context, 4, source.points.length, 8);
                const vector12:uint = as3flatbuffers.Pack.startVector(context, source.points.length);
                as3flatbuffers.Pack.patchOffset(context, offset12, vector12);
                const data12:uint = as3flatbuffers.Pack.reserve(context, source.points.length * 8);
                for (var index12:uint = 0; index12 < source.points.length; index12++)
                {
                    if (source.points[index12] == null)
                        throw new ArgumentError("points elements must be non-null");
                    const element12:uint = data12 + index12 * 8;
                    si32(source.points[index12].x, element12 + 0);
                    si32(source.points[index12].y, element12 + 4);
                }
            }
            if (offset13)
            {
                as3flatbuffers.Pack.prepareVector(context, 16, source.aligned.length, 16);
                const vector13:uint = as3flatbuffers.Pack.startVector(context, source.aligned.length);
                as3flatbuffers.Pack.patchOffset(context, offset13, vector13);
                const data13:uint = as3flatbuffers.Pack.reserve(context, source.aligned.length * 16);
                for (var index13:uint = 0; index13 < source.aligned.length; index13++)
                {
                    if (source.aligned[index13] == null)
                        throw new ArgumentError("aligned elements must be non-null");
                    const element13:uint = data13 + index13 * 16;
                    if (!source.aligned[index13].point)
                        throw new ArgumentError("source.aligned[index13].point must be non-null");
                    si32(source.aligned[index13].point.x, element13 + 0);
                    si32(source.aligned[index13].point.y, element13 + 4);
                    sf64(source.aligned[index13].weight, element13 + 8);
                }
            }
            if (offset14)
            {
                as3flatbuffers.Pack.prepareVector(context, 4, source.entries.length, 4);
                const vector14:uint = as3flatbuffers.Pack.startVector(context, source.entries.length);
                as3flatbuffers.Pack.patchOffset(context, offset14, vector14);
                const data14:uint = as3flatbuffers.Pack.reserve(context, source.entries.length * 4);
                for (var index14:uint = 0; index14 < source.entries.length; index14++)
                {
                    if (source.entries[index14] == null)
                        throw new ArgumentError("entries elements must be non-null");
                    const child14:uint = fixtures.vectors.Entry.packInto(source.entries[index14], context);
                    as3flatbuffers.Pack.patchOffset(context, data14 + index14 * 4, child14);
                }
            }
            return table;
        }

        /** Decode a FlatBuffer whose root-offset word starts at offset. */
        public static function unpack(bytes:flash.utils.ByteArray, destination:Vectors = null, offset:uint = 0):Vectors
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.begin(context, bytes);
            try
            {
                destination = unpackFrom(context, as3flatbuffers.Unpack.root(context, offset), destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }

        /** Internal generated entry point; context must already own domain memory. */
        public static function unpackFrom(context:as3flatbuffers.UnpackContext, base:uint, destination:Vectors):Vectors
        {
            const vtable:uint = as3flatbuffers.Unpack.vtable(context, base);
            const vtableSize:uint = li16(vtable);
            const objectSize:uint = li16(vtable + 2);
            if (!destination)
                destination = new Vectors();

            const field0:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 4, 4);
            const vector0:uint = as3flatbuffers.Unpack.vector(context, field0, 1);
            const count0:uint = vector0 ? uint(li32(vector0)) : 0;
            destination.flags.length = count0;

            for (var index0:uint = 0; index0 < count0; index0++)
            {
                const element0:uint = vector0 + 4 + index0 * 1;
                destination.flags[index0] = (li8(element0) != 0);
            }

            const field1:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 6, 4);
            const vector1:uint = as3flatbuffers.Unpack.vector(context, field1, 1);
            const count1:uint = vector1 ? uint(li32(vector1)) : 0;
            destination.signedBytes.length = count1;

            for (var index1:uint = 0; index1 < count1; index1++)
            {
                const element1:uint = vector1 + 4 + index1 * 1;
                destination.signedBytes[index1] = sxi8(li8(element1));
            }

            const field2:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 8, 4);
            const vector2:uint = as3flatbuffers.Unpack.vector(context, field2, 1);
            const count2:uint = vector2 ? uint(li32(vector2)) : 0;
            destination.unsignedBytes.length = count2;

            for (var index2:uint = 0; index2 < count2; index2++)
            {
                const element2:uint = vector2 + 4 + index2 * 1;
                destination.unsignedBytes[index2] = li8(element2);
            }

            const field3:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 10, 4);
            const vector3:uint = as3flatbuffers.Unpack.vector(context, field3, 2);
            const count3:uint = vector3 ? uint(li32(vector3)) : 0;
            destination.signedShorts.length = count3;

            for (var index3:uint = 0; index3 < count3; index3++)
            {
                const element3:uint = vector3 + 4 + index3 * 2;
                destination.signedShorts[index3] = sxi16(li16(element3));
            }

            const field4:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 12, 4);
            const vector4:uint = as3flatbuffers.Unpack.vector(context, field4, 2);
            const count4:uint = vector4 ? uint(li32(vector4)) : 0;
            destination.unsignedShorts.length = count4;

            for (var index4:uint = 0; index4 < count4; index4++)
            {
                const element4:uint = vector4 + 4 + index4 * 2;
                destination.unsignedShorts[index4] = li16(element4);
            }

            const field5:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 14, 4);
            const vector5:uint = as3flatbuffers.Unpack.vector(context, field5, 4);
            const count5:uint = vector5 ? uint(li32(vector5)) : 0;
            destination.ints.length = count5;

            for (var index5:uint = 0; index5 < count5; index5++)
            {
                const element5:uint = vector5 + 4 + index5 * 4;
                destination.ints[index5] = li32(element5);
            }

            const field6:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 16, 4);
            const vector6:uint = as3flatbuffers.Unpack.vector(context, field6, 4);
            const count6:uint = vector6 ? uint(li32(vector6)) : 0;
            destination.uints.length = count6;

            for (var index6:uint = 0; index6 < count6; index6++)
            {
                const element6:uint = vector6 + 4 + index6 * 4;
                destination.uints[index6] = uint(li32(element6));
            }

            const field7:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 18, 4);
            const vector7:uint = as3flatbuffers.Unpack.vector(context, field7, 8);
            const count7:uint = vector7 ? uint(li32(vector7)) : 0;
            destination.longs.length = count7;

            for (var index7:uint = 0; index7 < count7; index7++)
            {
                const element7:uint = vector7 + 4 + index7 * 8;
                if (!destination.longs[index7])
                    destination.longs[index7] = new as3flatbuffers.types.Int64();
                destination.longs[index7].set(uint(li32(element7)), li32(element7 + 4));
            }

            const field8:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 20, 4);
            const vector8:uint = as3flatbuffers.Unpack.vector(context, field8, 8);
            const count8:uint = vector8 ? uint(li32(vector8)) : 0;
            destination.ulongs.length = count8;

            for (var index8:uint = 0; index8 < count8; index8++)
            {
                const element8:uint = vector8 + 4 + index8 * 8;
                if (!destination.ulongs[index8])
                    destination.ulongs[index8] = new as3flatbuffers.types.UInt64();
                destination.ulongs[index8].set(uint(li32(element8)), uint(li32(element8 + 4)));
            }

            const field9:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 22, 4);
            const vector9:uint = as3flatbuffers.Unpack.vector(context, field9, 4);
            const count9:uint = vector9 ? uint(li32(vector9)) : 0;
            destination.floats.length = count9;

            for (var index9:uint = 0; index9 < count9; index9++)
            {
                const element9:uint = vector9 + 4 + index9 * 4;
                destination.floats[index9] = lf32(element9);
            }

            const field10:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 24, 4);
            const vector10:uint = as3flatbuffers.Unpack.vector(context, field10, 8);
            const count10:uint = vector10 ? uint(li32(vector10)) : 0;
            destination.doubles.length = count10;

            for (var index10:uint = 0; index10 < count10; index10++)
            {
                const element10:uint = vector10 + 4 + index10 * 8;
                destination.doubles[index10] = lf64(element10);
            }

            const field11:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 26, 4);
            const vector11:uint = as3flatbuffers.Unpack.vector(context, field11, 4);
            const count11:uint = vector11 ? uint(li32(vector11)) : 0;
            destination.texts.length = count11;

            for (var index11:uint = 0; index11 < count11; index11++)
            {
                const element11:uint = vector11 + 4 + index11 * 4;
                destination.texts[index11] = as3flatbuffers.Unpack.stringValue(context, element11);
            }

            const field12:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 28, 4);
            const vector12:uint = as3flatbuffers.Unpack.vector(context, field12, 8);
            const count12:uint = vector12 ? uint(li32(vector12)) : 0;
            destination.points.length = count12;

            for (var index12:uint = 0; index12 < count12; index12++)
            {
                const element12:uint = vector12 + 4 + index12 * 8;
                destination.points[index12] = fixtures.vectors.Pair.unpackFrom(context, element12, destination.points[index12]);
            }

            const field13:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 30, 4);
            const vector13:uint = as3flatbuffers.Unpack.vector(context, field13, 16);
            const count13:uint = vector13 ? uint(li32(vector13)) : 0;
            destination.aligned.length = count13;

            for (var index13:uint = 0; index13 < count13; index13++)
            {
                const element13:uint = vector13 + 4 + index13 * 16;
                destination.aligned[index13] = fixtures.vectors.Aligned.unpackFrom(context, element13, destination.aligned[index13]);
            }

            const field14:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 32, 4);
            const vector14:uint = as3flatbuffers.Unpack.vector(context, field14, 4);
            const count14:uint = vector14 ? uint(li32(vector14)) : 0;
            destination.entries.length = count14;

            for (var index14:uint = 0; index14 < count14; index14++)
            {
                const element14:uint = vector14 + 4 + index14 * 4;
                const child14:uint = as3flatbuffers.Unpack.tableOffset(context, element14);
                destination.entries[index14] = fixtures.vectors.Entry.unpackFrom(context, child14, destination.entries[index14]);
            }

            const position15:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 34, 4);
            if (!position15)
            {
                destination.intsPresent = 0;
            }
            else
            {
                destination.intsPresent = li32(position15);
            }

            const position16:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 36, 4);
            if (!position16)
            {
                destination.entriesView = 0;
            }
            else
            {
                destination.entriesView = li32(position16);
            }
            return destination;
        }
    }
}
