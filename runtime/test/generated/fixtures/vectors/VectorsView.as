// Code generated by as3flatc. DO NOT EDIT.
package fixtures.vectors
{
    import as3flatbuffers.TableView;
    import flash.utils.ByteArray;
    import as3flatbuffers.types.Int64;
    import as3flatbuffers.types.UInt64;
    import fixtures.vectors.Aligned;
    import fixtures.vectors.AlignedView;
    import fixtures.vectors.Entry;
    import fixtures.vectors.EntryView;
    import fixtures.vectors.Pair;
    import fixtures.vectors.PairView;

    /** Borrowed read-only view; unpack() produces independent owned values. */
    public final class VectorsView extends as3flatbuffers.TableView
    {
        private const pointsView:fixtures.vectors.PairView = new fixtures.vectors.PairView();

        private const alignedView:fixtures.vectors.AlignedView = new fixtures.vectors.AlignedView();

        private var entriesView_:fixtures.vectors.EntryView;

        public function get flagsLength():uint
        {
            const position:uint = vectorOffset(4, 1);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function flags(index:uint):Boolean
        {
            const vector:uint = vectorOffset(4, 1);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 1;
            bytes.position = position;
            return bytes.readBoolean();
        }

        public function get signedBytesLength():uint
        {
            const position:uint = vectorOffset(6, 1);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function signedBytes(index:uint):int
        {
            const vector:uint = vectorOffset(6, 1);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 1;
            bytes.position = position;
            return bytes.readByte();
        }

        public function get unsignedBytesLength():uint
        {
            const position:uint = vectorOffset(8, 1);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function unsignedBytes(index:uint):uint
        {
            const vector:uint = vectorOffset(8, 1);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 1;
            bytes.position = position;
            return bytes.readUnsignedByte();
        }

        public function get signedShortsLength():uint
        {
            const position:uint = vectorOffset(10, 2);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function signedShorts(index:uint):int
        {
            const vector:uint = vectorOffset(10, 2);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 2;
            bytes.position = position;
            return bytes.readShort();
        }

        public function get unsignedShortsLength():uint
        {
            const position:uint = vectorOffset(12, 2);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function unsignedShorts(index:uint):uint
        {
            const vector:uint = vectorOffset(12, 2);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 2;
            bytes.position = position;
            return bytes.readUnsignedShort();
        }

        public function get intsLength():uint
        {
            const position:uint = vectorOffset(14, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function ints(index:uint):int
        {
            const vector:uint = vectorOffset(14, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            bytes.position = position;
            return bytes.readInt();
        }

        public function get uintsLength():uint
        {
            const position:uint = vectorOffset(16, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function uints(index:uint):uint
        {
            const vector:uint = vectorOffset(16, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function get longsLength():uint
        {
            const position:uint = vectorOffset(18, 8);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function longs(index:uint):as3flatbuffers.types.Int64
        {
            const vector:uint = vectorOffset(18, 8);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 8;
            bytes.position = position;
            return new as3flatbuffers.types.Int64(bytes.readUnsignedInt(), bytes.readInt());
        }

        public function get ulongsLength():uint
        {
            const position:uint = vectorOffset(20, 8);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function ulongs(index:uint):as3flatbuffers.types.UInt64
        {
            const vector:uint = vectorOffset(20, 8);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 8;
            bytes.position = position;
            return new as3flatbuffers.types.UInt64(bytes.readUnsignedInt(), bytes.readUnsignedInt());
        }

        public function get floatsLength():uint
        {
            const position:uint = vectorOffset(22, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function floats(index:uint):Number
        {
            const vector:uint = vectorOffset(22, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            bytes.position = position;
            return bytes.readFloat();
        }

        public function get doublesLength():uint
        {
            const position:uint = vectorOffset(24, 8);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function doubles(index:uint):Number
        {
            const vector:uint = vectorOffset(24, 8);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 8;
            bytes.position = position;
            return bytes.readDouble();
        }

        public function get textsLength():uint
        {
            const position:uint = vectorOffset(26, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function texts(index:uint):String
        {
            const vector:uint = vectorOffset(26, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            return stringAt(position);
        }

        public function get pointsLength():uint
        {
            const position:uint = vectorOffset(28, 8);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function points(index:uint):fixtures.vectors.PairView
        {
            const vector:uint = vectorOffset(28, 8);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 8;
            return this.pointsView.bind(bytes, position);
        }

        public function get alignedLength():uint
        {
            const position:uint = vectorOffset(30, 16);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function aligned(index:uint):fixtures.vectors.AlignedView
        {
            const vector:uint = vectorOffset(30, 16);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 16;
            return this.alignedView.bind(bytes, position);
        }

        public function get entriesLength():uint
        {
            const position:uint = vectorOffset(32, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        /** Returns a cached view rebound on each access. */
        public function entries(index:uint):fixtures.vectors.EntryView
        {
            const vector:uint = vectorOffset(32, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            if (!this.entriesView_)
                this.entriesView_ = new fixtures.vectors.EntryView();

            const child:uint = referenceAt(position);
            this.entriesView_.bind(bytes, child);
            return this.entriesView_;
        }

        public function get intsPresent():int
        {
            const position:uint = fieldOffset(34, 4);
            if (!position)
                return 0;

            bytes.position = position;
            return bytes.readInt();
        }

        public function get entriesView():int
        {
            const position:uint = fieldOffset(36, 4);
            if (!position)
                return 0;

            bytes.position = position;
            return bytes.readInt();
        }
    }
}
