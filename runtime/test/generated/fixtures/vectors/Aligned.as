// Code generated by as3flatc. DO NOT EDIT.
package fixtures.vectors
{
    import as3flatbuffers.Pack;
    import as3flatbuffers.Unpack;
    import as3flatbuffers.UnpackContext;
    import avm2.intrinsics.memory.*;
    import flash.utils.ByteArray;
    import fixtures.vectors.Pair;

    /** Owned inline struct. pack() writes raw struct bytes into the destination. */
    public final class Aligned
    {
        private static const PACK:as3flatbuffers.PackContext = new as3flatbuffers.PackContext();
        private static const UNPACK:as3flatbuffers.UnpackContext = new as3flatbuffers.UnpackContext();

        public var point:fixtures.vectors.Pair = new fixtures.vectors.Pair();
        public var weight:Number = 0;

        public static function reset(msg:Aligned):void
        {
            fixtures.vectors.Pair.reset(msg.point);
            msg.weight = 0;
        }

        public static function clone(source:Aligned):Aligned
        {
            if (!source)
                return null;

            const destination:Aligned = new Aligned();
            destination.point = fixtures.vectors.Pair.clone(source.point);
            destination.weight = source.weight;

            return destination;
        }

        /** Replace dst with packed bytes. Caller must select little-endian. Returns dst at position zero. */
        public static function pack(source:Aligned, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.begin(context, dst, false);
                as3flatbuffers.Pack.finish(context, packInto(source, context));
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Write into an active context and return the absolute object offset. */
        public static function packInto(source:Aligned, context:as3flatbuffers.PackContext):uint
        {
            if (!source || !context)
                throw new ArgumentError("Source and context must be non-null");

            const bytes:flash.utils.ByteArray = as3flatbuffers.Pack.prepareStruct(context, 16, 16);
            const start:uint = bytes.position;

            if (!source.point)
                throw new ArgumentError("point must be non-null");

            bytes.writeInt(source.point.x);
            bytes.writeInt(source.point.y);
            bytes.writeDouble(source.weight);
            return start;
        }

        /** Decode raw struct bytes at offset into a new or reused owned value. */
        public static function unpack(bytes:flash.utils.ByteArray, destination:Aligned = null, offset:uint = 0):Aligned
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.begin(context, bytes);
            try
            {
                destination = unpackFrom(context, offset, destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }

        /** Internal generated entry point; context must already own domain memory. */
        public static function unpackFrom(context:as3flatbuffers.UnpackContext, base:uint, destination:Aligned):Aligned
        {
            as3flatbuffers.Unpack.struct(context, base, 16);
            if (!destination)
                destination = new Aligned();

            destination.point = fixtures.vectors.Pair.unpackFrom(context, base + 0, destination.point);

            destination.weight = lf64(base + 8);
            return destination;
        }
    }
}
