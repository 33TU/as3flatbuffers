// Code generated by as3flatc. DO NOT EDIT.
package fixtures.nested
{
    import as3flatbuffers.Pack;
    import as3flatbuffers.Unpack;
    import as3flatbuffers.UnpackContext;
    import avm2.intrinsics.memory.*;
    import flash.utils.ByteArray;
    import fixtures.nested.Left;
    import fixtures.nested.Node;

    /** Owned mutable value. pack() writes a complete FlatBuffer into the destination. */
    public final class Scene
    {
        private static const PACK:as3flatbuffers.PackContext = new as3flatbuffers.PackContext();
        private static const UNPACK:as3flatbuffers.UnpackContext = new as3flatbuffers.UnpackContext();

        public var head:fixtures.nested.Node = null;
        public var alternate:fixtures.nested.Node = null;
        public var pair:fixtures.nested.Left = null;
        public var serial:int = 0;

        public static function reset(msg:Scene):void
        {
            msg.head = null;
            msg.alternate = null;
            msg.pair = null;
            msg.serial = 0;
        }

        public static function clone(source:Scene):Scene
        {
            if (!source)
                return null;

            const destination:Scene = new Scene();
            destination.head = fixtures.nested.Node.clone(source.head);
            destination.alternate = fixtures.nested.Node.clone(source.alternate);
            destination.pair = fixtures.nested.Left.clone(source.pair);
            destination.serial = source.serial;

            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function pack(source:Scene, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.begin(context, dst, true);
                as3flatbuffers.Pack.finish(context, packInto(source, context));
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Write into an active context and return the absolute object offset. */
        public static function packInto(source:Scene, context:as3flatbuffers.PackContext):uint
        {
            if (!source || !context)
                throw new ArgumentError("Source and context must be non-null");

            as3flatbuffers.Pack.ensure(context, 48);
            as3flatbuffers.Pack.prepare(context, 2);
            as3flatbuffers.Pack.reserveVtable(context, 4);
            as3flatbuffers.Pack.prepare(context, 4);
            as3flatbuffers.Pack.startTable(context);
            const offset0:uint = source.head ? as3flatbuffers.Pack.reserveOffset(context, 0) : 0;
            const offset1:uint = source.alternate ? as3flatbuffers.Pack.reserveOffset(context, 1) : 0;
            const offset2:uint = source.pair ? as3flatbuffers.Pack.reserveOffset(context, 2) : 0;
            if (source.serial != 0)
            {
                as3flatbuffers.Pack.addInt32(context, 3, source.serial);
            }
            const table:uint = as3flatbuffers.Pack.endTable(context);
            if (offset0)
                as3flatbuffers.Pack.patchOffset(context, offset0, fixtures.nested.Node.packInto(source.head, context));
            if (offset1)
                as3flatbuffers.Pack.patchOffset(context, offset1, fixtures.nested.Node.packInto(source.alternate, context));
            if (offset2)
                as3flatbuffers.Pack.patchOffset(context, offset2, fixtures.nested.Left.packInto(source.pair, context));
            return table;
        }

        /** Decode a FlatBuffer whose root-offset word starts at offset. */
        public static function unpack(bytes:flash.utils.ByteArray, destination:Scene = null, offset:uint = 0):Scene
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.begin(context, bytes);
            try
            {
                destination = unpackFrom(context, as3flatbuffers.Unpack.root(context, offset), destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }

        /** Internal generated entry point; context must already own domain memory. */
        public static function unpackFrom(context:as3flatbuffers.UnpackContext, base:uint, destination:Scene):Scene
        {
            const vtable:uint = as3flatbuffers.Unpack.vtable(context, base);
            const vtableSize:uint = li16(vtable);
            const objectSize:uint = li16(vtable + 2);
            if (!destination)
                destination = new Scene();

            const field0:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 4, 4);
            const position0:uint = as3flatbuffers.Unpack.tableOffset(context, field0);
            destination.head = position0 ? fixtures.nested.Node.unpackFrom(context, position0, destination.head) : null;

            const field1:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 6, 4);
            const position1:uint = as3flatbuffers.Unpack.tableOffset(context, field1);
            destination.alternate = position1 ? fixtures.nested.Node.unpackFrom(context, position1, destination.alternate) : null;

            const field2:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 8, 4);
            const position2:uint = as3flatbuffers.Unpack.tableOffset(context, field2);
            destination.pair = position2 ? fixtures.nested.Left.unpackFrom(context, position2, destination.pair) : null;

            const position3:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 10, 4);
            if (!position3)
            {
                destination.serial = 0;
            }
            else
            {
                destination.serial = li32(position3);
            }
            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function packSizePrefixed(source:Scene, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.beginRoot(context, dst, true, false, 0);
                const root:uint = packInto(source, context);
                as3flatbuffers.Pack.finishRoot(context, root, true);
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Decode one size-prefixed frame at offset; references are bounded to its declared length. */
        public static function unpackSizePrefixed(bytes:flash.utils.ByteArray, destination:Scene = null, offset:uint = 0):Scene
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.beginSizePrefixed(context, bytes, offset);
            try
            {
                destination = unpackFrom(context, as3flatbuffers.Unpack.root(context, 4), destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }
    }
}
