// Code generated by as3flatc. DO NOT EDIT.
package fixtures.nested
{
    import as3flatbuffers.TableView;
    import flash.utils.ByteArray;
    import fixtures.nested.Node;
    import fixtures.nested.NodeView;
    import fixtures.nested.Position;
    import fixtures.nested.PositionView;

    /** Borrowed read-only view; unpack() produces independent owned values. */
    public final class NodeView extends as3flatbuffers.TableView
    {
        private var nextView:fixtures.nested.NodeView;

        private const positionView:fixtures.nested.PositionView = new fixtures.nested.PositionView();

        private var branchView:fixtures.nested.NodeView;

        public function get value():int
        {
            const position:uint = fieldOffset(4, 4);
            if (!position)
                return 0;

            bytes.position = position;
            return bytes.readInt();
        }

        public function get next():fixtures.nested.NodeView
        {
            const position:uint = tableOffset(6);
            if (!position)
                return null;

            if (!this.nextView)
                this.nextView = new fixtures.nested.NodeView();

            this.nextView.bind(bytes, position);
            return this.nextView;
        }

        public function get position():fixtures.nested.PositionView
        {
            const position:uint = fieldOffset(8, 16);
            return position ? this.positionView.bind(bytes, position) : null;
        }

        public function get branch():fixtures.nested.NodeView
        {
            const position:uint = tableOffset(10);
            if (!position)
                return null;

            if (!this.branchView)
                this.branchView = new fixtures.nested.NodeView();

            this.branchView.bind(bytes, position);
            return this.branchView;
        }

        public static function unpack(source:NodeView, destination:Node = null):Node
        {
            if (!source || !source.bytes)
                throw new Error("View is not bound");

            const bytes:flash.utils.ByteArray = source.bytes;
            if (!destination)
                destination = new Node();

            const position0:uint = source.fieldOffset(4, 4);
            if (!position0)
            {
                destination.value = 0;
            }
            else
            {
                bytes.position = position0;
                destination.value = bytes.readInt();
            }

            const nextViewPosition:uint = source.tableOffset(6);
            if (!nextViewPosition)
            {
                destination.next = null;
            }
            else
            {
                if (!source.nextView)
                    source.nextView = new fixtures.nested.NodeView();

                source.nextView.bind(bytes, nextViewPosition);
                destination.next = fixtures.nested.NodeView.unpack(source.nextView, destination.next);
            }

            const positionViewPosition:uint = source.fieldOffset(8, 16);
            if (!positionViewPosition)
            {
                destination.position = null;
            }
            else
            {
                destination.position = fixtures.nested.PositionView.unpack(source.positionView.bind(bytes, positionViewPosition), destination.position);
            }

            const branchViewPosition:uint = source.tableOffset(10);
            if (!branchViewPosition)
            {
                destination.branch = null;
            }
            else
            {
                if (!source.branchView)
                    source.branchView = new fixtures.nested.NodeView();

                source.branchView.bind(bytes, branchViewPosition);
                destination.branch = fixtures.nested.NodeView.unpack(source.branchView, destination.branch);
            }
            return destination;
        }
    }
}
