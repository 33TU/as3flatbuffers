// Code generated by as3flatc. DO NOT EDIT.
package fixtures.geometry
{
    import as3flatbuffers.Pack;
    import as3flatbuffers.Unpack;
    import as3flatbuffers.UnpackContext;
    import avm2.intrinsics.memory.*;
    import flash.utils.ByteArray;
    import as3flatbuffers.types.Int64;
    import as3flatbuffers.types.UInt64;
    import fixtures.geometry.Point;

    /** Owned inline struct. pack() writes raw struct bytes into the destination. */
    public final class Frame
    {
        private static const PACK:as3flatbuffers.PackContext = new as3flatbuffers.PackContext();
        private static const UNPACK:as3flatbuffers.UnpackContext = new as3flatbuffers.UnpackContext();

        public var tag:uint = 0;
        public var point:fixtures.geometry.Point = new fixtures.geometry.Point();
        public var count:int = 0;
        public var signedValue:as3flatbuffers.types.Int64 = new as3flatbuffers.types.Int64(0, 0);
        public var unsignedValue:as3flatbuffers.types.UInt64 = new as3flatbuffers.types.UInt64(0, 0);
        public var weight:Number = 0;
        public var enabled:Boolean = false;
        public var tiny:int = 0;
        public var small:uint = 0;
        public var number:int = 0;
        public var unsignedNumber:uint = 0;
        public var fraction:Number = 0;

        public static function reset(msg:Frame):void
        {
            msg.tag = 0;
            fixtures.geometry.Point.reset(msg.point);
            msg.count = 0;
            msg.signedValue.reset();
            msg.unsignedValue.reset();
            msg.weight = 0;
            msg.enabled = false;
            msg.tiny = 0;
            msg.small = 0;
            msg.number = 0;
            msg.unsignedNumber = 0;
            msg.fraction = 0;
        }

        public static function clone(source:Frame):Frame
        {
            if (!source)
                return null;

            const destination:Frame = new Frame();
            destination.tag = source.tag;
            destination.point = fixtures.geometry.Point.clone(source.point);
            destination.count = source.count;
            destination.signedValue.copyFrom(source.signedValue);
            destination.unsignedValue.copyFrom(source.unsignedValue);
            destination.weight = source.weight;
            destination.enabled = source.enabled;
            destination.tiny = source.tiny;
            destination.small = source.small;
            destination.number = source.number;
            destination.unsignedNumber = source.unsignedNumber;
            destination.fraction = source.fraction;

            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function pack(source:Frame, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.begin(context, dst, false);
                as3flatbuffers.Pack.finish(context, packInto(source, context));
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Write into an active context and return the absolute object offset. */
        public static function packInto(source:Frame, context:as3flatbuffers.PackContext):uint
        {
            if (!source || !context)
                throw new ArgumentError("Source and context must be non-null");

            const start:uint = as3flatbuffers.Pack.prepareStruct(context, 56, 8);
            si8(source.tag, start + 0);
            si16(0, start + 1);
            si8(0, start + 3);
            if (!source.point)
                throw new ArgumentError("source.point must be non-null");
            sf32(source.point.x, start + 4);
            sf32(source.point.y, start + 8);
            si16(source.count, start + 12);
            si16(0, start + 14);
            if (!source.signedValue)
                throw new ArgumentError("source.signedValue must be non-null");
            si32(source.signedValue.low, start + 16);
            si32(source.signedValue.high, start + 16 + 4);
            if (!source.unsignedValue)
                throw new ArgumentError("source.unsignedValue must be non-null");
            si32(source.unsignedValue.low, start + 24);
            si32(source.unsignedValue.high, start + 24 + 4);
            sf64(source.weight, start + 32);
            si8((source.enabled ? 1 : 0), start + 40);
            si8(source.tiny, start + 41);
            si16(source.small, start + 42);
            si32(source.number, start + 44);
            si32(source.unsignedNumber, start + 48);
            sf32(source.fraction, start + 52);
            return start;
        }

        /** Decode raw struct bytes at offset into a new or reused owned value. */
        public static function unpack(bytes:flash.utils.ByteArray, destination:Frame = null, offset:uint = 0):Frame
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.begin(context, bytes);
            try
            {
                destination = unpackFrom(context, offset, destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }

        /** Internal generated entry point; context must already own domain memory. */
        public static function unpackFrom(context:as3flatbuffers.UnpackContext, base:uint, destination:Frame):Frame
        {
            as3flatbuffers.Unpack.struct(context, base, 56);
            if (!destination)
                destination = new Frame();

            destination.tag = li8(base + 0);

            destination.point = fixtures.geometry.Point.unpackFrom(context, base + 4, destination.point);

            destination.count = sxi16(li16(base + 12));

            if (!destination.signedValue)
                destination.signedValue = new as3flatbuffers.types.Int64();

            destination.signedValue.set(uint(li32(base + 16)), li32(base + 20));

            if (!destination.unsignedValue)
                destination.unsignedValue = new as3flatbuffers.types.UInt64();

            destination.unsignedValue.set(uint(li32(base + 24)), uint(li32(base + 28)));

            destination.weight = lf64(base + 32);

            destination.enabled = (li8(base + 40) != 0);

            destination.tiny = sxi8(li8(base + 41));

            destination.small = li16(base + 42);

            destination.number = li32(base + 44);

            destination.unsignedNumber = uint(li32(base + 48));

            destination.fraction = lf32(base + 52);
            return destination;
        }
    }
}
