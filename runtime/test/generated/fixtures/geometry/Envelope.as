// Code generated by as3flatc. DO NOT EDIT.
package fixtures.geometry
{
    import as3flatbuffers.Pack;
    import as3flatbuffers.Unpack;
    import as3flatbuffers.UnpackContext;
    import avm2.intrinsics.memory.*;
    import flash.utils.ByteArray;
    import fixtures.geometry.Frame;

    /** Owned inline struct. pack() writes raw struct bytes into the destination. */
    public final class Envelope
    {
        private static const PACK:as3flatbuffers.PackContext = new as3flatbuffers.PackContext();
        private static const UNPACK:as3flatbuffers.UnpackContext = new as3flatbuffers.UnpackContext();

        public var lead:int = 0;
        public var frame:fixtures.geometry.Frame = new fixtures.geometry.Frame();
        public var tail:int = 0;

        public static function reset(msg:Envelope):void
        {
            msg.lead = 0;
            fixtures.geometry.Frame.reset(msg.frame);
            msg.tail = 0;
        }

        public static function clone(source:Envelope):Envelope
        {
            if (!source)
                return null;

            const destination:Envelope = new Envelope();
            destination.lead = source.lead;
            destination.frame = fixtures.geometry.Frame.clone(source.frame);
            destination.tail = source.tail;

            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function pack(source:Envelope, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.begin(context, dst, false);
                as3flatbuffers.Pack.finish(context, packInto(source, context));
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Write into an active context and return the absolute object offset. */
        public static function packInto(source:Envelope, context:as3flatbuffers.PackContext):uint
        {
            if (!source || !context)
                throw new ArgumentError("Source and context must be non-null");

            const start:uint = as3flatbuffers.Pack.prepareStruct(context, 72, 8);
            si8(source.lead, start + 0);
            si32(0, start + 1);
            si16(0, start + 5);
            si8(0, start + 7);
            if (!source.frame)
                throw new ArgumentError("source.frame must be non-null");
            si8(source.frame.tag, start + 8);
            si16(0, start + 9);
            si8(0, start + 11);
            if (!source.frame.point)
                throw new ArgumentError("source.frame.point must be non-null");
            sf32(source.frame.point.x, start + 12);
            sf32(source.frame.point.y, start + 16);
            si16(source.frame.count, start + 20);
            si16(0, start + 22);
            if (!source.frame.signedValue)
                throw new ArgumentError("source.frame.signedValue must be non-null");
            si32(source.frame.signedValue.low, start + 24);
            si32(source.frame.signedValue.high, start + 24 + 4);
            if (!source.frame.unsignedValue)
                throw new ArgumentError("source.frame.unsignedValue must be non-null");
            si32(source.frame.unsignedValue.low, start + 32);
            si32(source.frame.unsignedValue.high, start + 32 + 4);
            sf64(source.frame.weight, start + 40);
            si8((source.frame.enabled ? 1 : 0), start + 48);
            si8(source.frame.tiny, start + 49);
            si16(source.frame.small, start + 50);
            si32(source.frame.number, start + 52);
            si32(source.frame.unsignedNumber, start + 56);
            sf32(source.frame.fraction, start + 60);
            si16(source.tail, start + 64);
            si32(0, start + 66);
            si16(0, start + 70);
            return start;
        }

        /** Decode raw struct bytes at offset into a new or reused owned value. */
        public static function unpack(bytes:flash.utils.ByteArray, destination:Envelope = null, offset:uint = 0):Envelope
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.begin(context, bytes);
            try
            {
                destination = unpackFrom(context, offset, destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }

        /** Internal generated entry point; context must already own domain memory. */
        public static function unpackFrom(context:as3flatbuffers.UnpackContext, base:uint, destination:Envelope):Envelope
        {
            as3flatbuffers.Unpack.struct(context, base, 72);
            if (!destination)
                destination = new Envelope();

            destination.lead = sxi8(li8(base + 0));

            destination.frame = fixtures.geometry.Frame.unpackFrom(context, base + 8, destination.frame);

            destination.tail = sxi16(li16(base + 64));
            return destination;
        }
    }
}
