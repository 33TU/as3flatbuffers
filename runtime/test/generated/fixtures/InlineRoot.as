// Code generated by as3flatc. DO NOT EDIT.
package fixtures
{
    import as3flatbuffers.Pack;
    import as3flatbuffers.Unpack;
    import as3flatbuffers.UnpackContext;
    import avm2.intrinsics.memory.*;
    import flash.utils.ByteArray;
    import fixtures.geometry.Aligned;
    import fixtures.geometry.Envelope;
    import fixtures.geometry.Frame;
    import fixtures.geometry.Point;

    /** Owned mutable value. pack() writes a complete FlatBuffer into the destination. */
    public final class InlineRoot
    {
        private static const PACK:as3flatbuffers.PackContext = new as3flatbuffers.PackContext();
        private static const UNPACK:as3flatbuffers.UnpackContext = new as3flatbuffers.UnpackContext();

        public var point:fixtures.geometry.Point = null;
        public var frame:fixtures.geometry.Frame = null;
        public var envelope:fixtures.geometry.Envelope = null;
        public var aligned:fixtures.geometry.Aligned = null;
        public var label_:int = 0;
        public var pointView:int = 0;

        public static function reset(msg:InlineRoot):void
        {
            msg.point = null;
            msg.frame = null;
            msg.envelope = null;
            msg.aligned = null;
            msg.label_ = 0;
            msg.pointView = 0;
        }

        public static function clone(source:InlineRoot):InlineRoot
        {
            if (!source)
                return null;

            const destination:InlineRoot = new InlineRoot();
            destination.point = fixtures.geometry.Point.clone(source.point);
            destination.frame = fixtures.geometry.Frame.clone(source.frame);
            destination.envelope = fixtures.geometry.Envelope.clone(source.envelope);
            destination.aligned = fixtures.geometry.Aligned.clone(source.aligned);
            destination.label_ = source.label_;
            destination.pointView = source.pointView;

            return destination;
        }

        /** Replace dst with packed bytes. Caller must select little-endian. Returns dst at position zero. */
        public static function pack(source:InlineRoot, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.begin(context, dst, true);
                as3flatbuffers.Pack.finish(context, packInto(source, context));
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Write into an active context and return the absolute object offset. */
        public static function packInto(source:InlineRoot, context:as3flatbuffers.PackContext):uint
        {
            if (!source || !context)
                throw new ArgumentError("Source and context must be non-null");

            as3flatbuffers.Pack.prepare(context, 2);
            as3flatbuffers.Pack.reserveVtable(context, 6);
            as3flatbuffers.Pack.prepare(context, 16);
            as3flatbuffers.Pack.startTable(context);
            if (source.point)
                as3flatbuffers.Pack.addStruct(context, 0, fixtures.geometry.Point.packInto(source.point, context));
            if (source.frame)
                as3flatbuffers.Pack.addStruct(context, 1, fixtures.geometry.Frame.packInto(source.frame, context));
            if (source.envelope)
                as3flatbuffers.Pack.addStruct(context, 2, fixtures.geometry.Envelope.packInto(source.envelope, context));
            if (source.aligned)
                as3flatbuffers.Pack.addStruct(context, 3, fixtures.geometry.Aligned.packInto(source.aligned, context));
            if (source.label_ != 0)
            {
                as3flatbuffers.Pack.addInt32(context, 4, source.label_);
            }
            if (source.pointView != 0)
            {
                as3flatbuffers.Pack.addInt32(context, 5, source.pointView);
            }
            return as3flatbuffers.Pack.endTable(context);
        }

        /** Decode a FlatBuffer whose root-offset word starts at offset. */
        public static function unpack(bytes:flash.utils.ByteArray, destination:InlineRoot = null, offset:uint = 0):InlineRoot
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.begin(context, bytes);
            try
            {
                destination = unpackFrom(context, as3flatbuffers.Unpack.root(context, offset), destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }

        /** Internal generated entry point; context must already own domain memory. */
        public static function unpackFrom(context:as3flatbuffers.UnpackContext, base:uint, destination:InlineRoot):InlineRoot
        {
            const vtable:uint = as3flatbuffers.Unpack.vtable(context, base);
            const vtableSize:uint = li16(vtable);
            const objectSize:uint = li16(vtable + 2);
            if (!destination)
                destination = new InlineRoot();

            const position0:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 4, 8);
            destination.point = position0 ? fixtures.geometry.Point.unpackFrom(context, position0, destination.point) : null;

            const position1:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 6, 56);
            destination.frame = position1 ? fixtures.geometry.Frame.unpackFrom(context, position1, destination.frame) : null;

            const position2:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 8, 72);
            destination.envelope = position2 ? fixtures.geometry.Envelope.unpackFrom(context, position2, destination.envelope) : null;

            const position3:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 10, 16);
            destination.aligned = position3 ? fixtures.geometry.Aligned.unpackFrom(context, position3, destination.aligned) : null;

            const position4:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 12, 4);
            if (!position4)
            {
                destination.label_ = 0;
            }
            else
            {
                destination.label_ = li32(position4);
            }

            const position5:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 14, 4);
            if (!position5)
            {
                destination.pointView = 0;
            }
            else
            {
                destination.pointView = li32(position5);
            }
            return destination;
        }
    }
}
