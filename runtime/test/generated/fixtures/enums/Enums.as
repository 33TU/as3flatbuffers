// Code generated by as3flatc. DO NOT EDIT.
package fixtures.enums
{
    import as3flatbuffers.Pack;
    import as3flatbuffers.Unpack;
    import as3flatbuffers.UnpackContext;
    import avm2.intrinsics.memory.*;
    import flash.utils.ByteArray;
    import as3flatbuffers.types.Int64;
    import as3flatbuffers.types.UInt64;
    import as3flatbuffers.types.OptionalUint;
    import fixtures.enums.EnumStruct;

    /** Owned mutable value. pack() writes a complete FlatBuffer into the destination. */
    public final class Enums
    {
        private static const PACK:as3flatbuffers.PackContext = new as3flatbuffers.PackContext();
        private static const UNPACK:as3flatbuffers.UnpackContext = new as3flatbuffers.UnpackContext();

        public var a:int = -128;
        public var b:uint = 0;
        public var c:int = -32768;
        public var d:uint = 0;
        public var e:int = -2147483648;
        public var f:uint = 0;
        public var g:as3flatbuffers.types.Int64 = new as3flatbuffers.types.Int64(1, 2097152);
        public var h:as3flatbuffers.types.UInt64 = new as3flatbuffers.types.UInt64(0, 0);
        public var av:Vector.<int> = new Vector.<int>();
        public var bv:Vector.<uint> = new Vector.<uint>();
        public var cv:Vector.<int> = new Vector.<int>();
        public var dv:Vector.<uint> = new Vector.<uint>();
        public var ev:Vector.<int> = new Vector.<int>();
        public var fv:Vector.<uint> = new Vector.<uint>();
        public var gv:Vector.<as3flatbuffers.types.Int64> = new Vector.<as3flatbuffers.types.Int64>();
        public var hv:Vector.<as3flatbuffers.types.UInt64> = new Vector.<as3flatbuffers.types.UInt64>();
        public var optionalMode:as3flatbuffers.types.OptionalUint = null;
        public var optionalWide:as3flatbuffers.types.UInt64 = null;
        public var state:fixtures.enums.EnumStruct = null;
        public var flags:uint = 0;

        public static function reset(msg:Enums):void
        {
            msg.a = -128;
            msg.b = 0;
            msg.c = -32768;
            msg.d = 0;
            msg.e = -2147483648;
            msg.f = 0;
            msg.g.set(1, 2097152);
            msg.h.reset();
            msg.av.length = 0;
            msg.bv.length = 0;
            msg.cv.length = 0;
            msg.dv.length = 0;
            msg.ev.length = 0;
            msg.fv.length = 0;
            msg.gv.length = 0;
            msg.hv.length = 0;
            msg.optionalMode = null;
            msg.optionalWide = null;
            msg.state = null;
            msg.flags = 0;
        }

        public static function clone(source:Enums):Enums
        {
            if (!source)
                return null;

            const destination:Enums = new Enums();
            destination.a = source.a;
            destination.b = source.b;
            destination.c = source.c;
            destination.d = source.d;
            destination.e = source.e;
            destination.f = source.f;
            destination.g.copyFrom(source.g);
            destination.h.copyFrom(source.h);
            destination.av = source.av.concat();
            destination.bv = source.bv.concat();
            destination.cv = source.cv.concat();
            destination.dv = source.dv.concat();
            destination.ev = source.ev.concat();
            destination.fv = source.fv.concat();
            destination.gv.length = source.gv.length;
            for (var index14:uint = 0; index14 < source.gv.length; index14++)
            {
                destination.gv[index14] = source.gv[index14] ? source.gv[index14].clone() : null;
            }
            destination.hv.length = source.hv.length;
            for (var index15:uint = 0; index15 < source.hv.length; index15++)
            {
                destination.hv[index15] = source.hv[index15] ? source.hv[index15].clone() : null;
            }
            destination.optionalMode = source.optionalMode ? source.optionalMode.clone() : null;
            destination.optionalWide = source.optionalWide ? source.optionalWide.clone() : null;
            destination.state = fixtures.enums.EnumStruct.clone(source.state);
            destination.flags = source.flags;

            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function pack(source:Enums, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.begin(context, dst, true);
                as3flatbuffers.Pack.finish(context, packInto(source, context));
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Write into an active context and return the absolute object offset. */
        public static function packInto(source:Enums, context:as3flatbuffers.PackContext):uint
        {
            if (!source || !context)
                throw new ArgumentError("Source and context must be non-null");

            as3flatbuffers.Pack.ensure(context, 226);
            as3flatbuffers.Pack.prepare(context, 2);
            as3flatbuffers.Pack.reserveVtable(context, 20);
            as3flatbuffers.Pack.prepare(context, 8);
            as3flatbuffers.Pack.startTable(context);
            if (source.a != -128)
            {
                as3flatbuffers.Pack.addInt8(context, 0, source.a);
            }
            if (source.b != 0)
            {
                as3flatbuffers.Pack.addUint8(context, 1, source.b);
            }
            if (source.c != -32768)
            {
                as3flatbuffers.Pack.prepare(context, 2);
                as3flatbuffers.Pack.addInt16(context, 2, source.c);
            }
            if (source.d != 0)
            {
                as3flatbuffers.Pack.prepare(context, 2);
                as3flatbuffers.Pack.addUint16(context, 3, source.d);
            }
            if (source.e != -2147483648)
            {
                as3flatbuffers.Pack.prepare(context, 4);
                as3flatbuffers.Pack.addInt32(context, 4, source.e);
            }
            if (source.f != 0)
            {
                as3flatbuffers.Pack.prepare(context, 4);
                as3flatbuffers.Pack.addUint32(context, 5, source.f);
            }
            if (!source.g)
                throw new ArgumentError("g must be non-null");
            if (source.g.low != 1 || source.g.high != 2097152)
            {
                as3flatbuffers.Pack.prepare(context, 8);
                as3flatbuffers.Pack.addInt64(context, 6, source.g);
            }
            if (!source.h)
                throw new ArgumentError("h must be non-null");
            if (source.h.low != 0 || source.h.high != 0)
            {
                as3flatbuffers.Pack.prepare(context, 8);
                as3flatbuffers.Pack.addUint64(context, 7, source.h);
            }
            var offset8:uint = 0;
            if (source.av.length)
            {
                as3flatbuffers.Pack.prepare(context, 4);
                offset8 = as3flatbuffers.Pack.reserveOffset(context, 8);
            }
            var offset9:uint = 0;
            if (source.bv.length)
            {
                as3flatbuffers.Pack.prepare(context, 4);
                offset9 = as3flatbuffers.Pack.reserveOffset(context, 9);
            }
            var offset10:uint = 0;
            if (source.cv.length)
            {
                as3flatbuffers.Pack.prepare(context, 4);
                offset10 = as3flatbuffers.Pack.reserveOffset(context, 10);
            }
            var offset11:uint = 0;
            if (source.dv.length)
            {
                as3flatbuffers.Pack.prepare(context, 4);
                offset11 = as3flatbuffers.Pack.reserveOffset(context, 11);
            }
            var offset12:uint = 0;
            if (source.ev.length)
            {
                as3flatbuffers.Pack.prepare(context, 4);
                offset12 = as3flatbuffers.Pack.reserveOffset(context, 12);
            }
            var offset13:uint = 0;
            if (source.fv.length)
            {
                as3flatbuffers.Pack.prepare(context, 4);
                offset13 = as3flatbuffers.Pack.reserveOffset(context, 13);
            }
            var offset14:uint = 0;
            if (source.gv.length)
            {
                as3flatbuffers.Pack.prepare(context, 4);
                offset14 = as3flatbuffers.Pack.reserveOffset(context, 14);
            }
            var offset15:uint = 0;
            if (source.hv.length)
            {
                as3flatbuffers.Pack.prepare(context, 4);
                offset15 = as3flatbuffers.Pack.reserveOffset(context, 15);
            }
            if (source.optionalMode)
            {
                as3flatbuffers.Pack.addUint8(context, 16, source.optionalMode.value);
            }
            if (source.optionalWide)
            {
                as3flatbuffers.Pack.prepare(context, 8);
                as3flatbuffers.Pack.addUint64(context, 17, source.optionalWide);
            }
            if (source.state)
                as3flatbuffers.Pack.addStruct(context, 18, fixtures.enums.EnumStruct.packInto(source.state, context));
            if (source.flags != 0)
            {
                as3flatbuffers.Pack.prepare(context, 4);
                as3flatbuffers.Pack.addUint32(context, 19, source.flags);
            }
            const table:uint = as3flatbuffers.Pack.endTable(context);
            if (offset8)
            {
                as3flatbuffers.Pack.prepareVector(context, 1, source.av.length, 1);
                const vector8:uint = as3flatbuffers.Pack.startVector(context, source.av.length);
                as3flatbuffers.Pack.patchOffset(context, offset8, vector8);
                const data8:uint = as3flatbuffers.Pack.reserve(context, source.av.length * 1);
                for (var index8:uint = 0; index8 < source.av.length; index8++)
                {
                    si8(source.av[index8], data8 + index8 * 1);
                }
            }
            if (offset9)
            {
                as3flatbuffers.Pack.prepareVector(context, 1, source.bv.length, 1);
                const vector9:uint = as3flatbuffers.Pack.startVector(context, source.bv.length);
                as3flatbuffers.Pack.patchOffset(context, offset9, vector9);
                const data9:uint = as3flatbuffers.Pack.reserve(context, source.bv.length * 1);
                for (var index9:uint = 0; index9 < source.bv.length; index9++)
                {
                    si8(source.bv[index9], data9 + index9 * 1);
                }
            }
            if (offset10)
            {
                as3flatbuffers.Pack.prepareVector(context, 2, source.cv.length, 2);
                const vector10:uint = as3flatbuffers.Pack.startVector(context, source.cv.length);
                as3flatbuffers.Pack.patchOffset(context, offset10, vector10);
                const data10:uint = as3flatbuffers.Pack.reserve(context, source.cv.length * 2);
                for (var index10:uint = 0; index10 < source.cv.length; index10++)
                {
                    si16(source.cv[index10], data10 + index10 * 2);
                }
            }
            if (offset11)
            {
                as3flatbuffers.Pack.prepareVector(context, 2, source.dv.length, 2);
                const vector11:uint = as3flatbuffers.Pack.startVector(context, source.dv.length);
                as3flatbuffers.Pack.patchOffset(context, offset11, vector11);
                const data11:uint = as3flatbuffers.Pack.reserve(context, source.dv.length * 2);
                for (var index11:uint = 0; index11 < source.dv.length; index11++)
                {
                    si16(source.dv[index11], data11 + index11 * 2);
                }
            }
            if (offset12)
            {
                as3flatbuffers.Pack.prepareVector(context, 4, source.ev.length, 4);
                const vector12:uint = as3flatbuffers.Pack.startVector(context, source.ev.length);
                as3flatbuffers.Pack.patchOffset(context, offset12, vector12);
                const data12:uint = as3flatbuffers.Pack.reserve(context, source.ev.length * 4);
                for (var index12:uint = 0; index12 < source.ev.length; index12++)
                {
                    si32(source.ev[index12], data12 + index12 * 4);
                }
            }
            if (offset13)
            {
                as3flatbuffers.Pack.prepareVector(context, 4, source.fv.length, 4);
                const vector13:uint = as3flatbuffers.Pack.startVector(context, source.fv.length);
                as3flatbuffers.Pack.patchOffset(context, offset13, vector13);
                const data13:uint = as3flatbuffers.Pack.reserve(context, source.fv.length * 4);
                for (var index13:uint = 0; index13 < source.fv.length; index13++)
                {
                    si32(source.fv[index13], data13 + index13 * 4);
                }
            }
            if (offset14)
            {
                as3flatbuffers.Pack.prepareVector(context, 8, source.gv.length, 8);
                const vector14:uint = as3flatbuffers.Pack.startVector(context, source.gv.length);
                as3flatbuffers.Pack.patchOffset(context, offset14, vector14);
                const data14:uint = as3flatbuffers.Pack.reserve(context, source.gv.length * 8);
                for (var index14:uint = 0; index14 < source.gv.length; index14++)
                {
                    if (source.gv[index14] == null)
                        throw new ArgumentError("gv elements must be non-null");
                    si32(source.gv[index14].low, data14 + index14 * 8);
                    si32(source.gv[index14].high, data14 + index14 * 8 + 4);
                }
            }
            if (offset15)
            {
                as3flatbuffers.Pack.prepareVector(context, 8, source.hv.length, 8);
                const vector15:uint = as3flatbuffers.Pack.startVector(context, source.hv.length);
                as3flatbuffers.Pack.patchOffset(context, offset15, vector15);
                const data15:uint = as3flatbuffers.Pack.reserve(context, source.hv.length * 8);
                for (var index15:uint = 0; index15 < source.hv.length; index15++)
                {
                    if (source.hv[index15] == null)
                        throw new ArgumentError("hv elements must be non-null");
                    si32(source.hv[index15].low, data15 + index15 * 8);
                    si32(source.hv[index15].high, data15 + index15 * 8 + 4);
                }
            }
            return table;
        }

        /** Decode a FlatBuffer whose root-offset word starts at offset. */
        public static function unpack(bytes:flash.utils.ByteArray, destination:Enums = null, offset:uint = 0):Enums
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.begin(context, bytes);
            try
            {
                destination = unpackFrom(context, as3flatbuffers.Unpack.root(context, offset), destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }

        /** Internal generated entry point; context must already own domain memory. */
        public static function unpackFrom(context:as3flatbuffers.UnpackContext, base:uint, destination:Enums):Enums
        {
            const vtable:uint = as3flatbuffers.Unpack.vtable(context, base);
            const vtableSize:uint = li16(vtable);
            const objectSize:uint = li16(vtable + 2);
            if (!destination)
                destination = new Enums();

            const position0:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 4, 1);
            if (!position0)
            {
                destination.a = -128;
            }
            else
            {
                destination.a = sxi8(li8(position0));
            }

            const position1:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 6, 1);
            if (!position1)
            {
                destination.b = 0;
            }
            else
            {
                destination.b = li8(position1);
            }

            const position2:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 8, 2);
            if (!position2)
            {
                destination.c = -32768;
            }
            else
            {
                destination.c = sxi16(li16(position2));
            }

            const position3:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 10, 2);
            if (!position3)
            {
                destination.d = 0;
            }
            else
            {
                destination.d = li16(position3);
            }

            const position4:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 12, 4);
            if (!position4)
            {
                destination.e = -2147483648;
            }
            else
            {
                destination.e = li32(position4);
            }

            const position5:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 14, 4);
            if (!position5)
            {
                destination.f = 0;
            }
            else
            {
                destination.f = uint(li32(position5));
            }

            const position6:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 16, 8);
            if (!destination.g)
                destination.g = new as3flatbuffers.types.Int64();

            if (!position6)
            {
                destination.g.set(1, 2097152);
            }
            else
            {
                destination.g.set(uint(li32(position6)), li32(position6 + 4));
            }

            const position7:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 18, 8);
            if (!destination.h)
                destination.h = new as3flatbuffers.types.UInt64();

            if (!position7)
            {
                destination.h.set(0, 0);
            }
            else
            {
                destination.h.set(uint(li32(position7)), uint(li32(position7 + 4)));
            }

            const field8:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 20, 4);
            const vector8:uint = as3flatbuffers.Unpack.vector(context, field8, 1);
            const count8:uint = vector8 ? uint(li32(vector8)) : 0;
            destination.av.length = count8;

            for (var index8:uint = 0; index8 < count8; index8++)
            {
                const element8:uint = vector8 + 4 + index8 * 1;
                destination.av[index8] = sxi8(li8(element8));
            }

            const field9:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 22, 4);
            const vector9:uint = as3flatbuffers.Unpack.vector(context, field9, 1);
            const count9:uint = vector9 ? uint(li32(vector9)) : 0;
            destination.bv.length = count9;

            for (var index9:uint = 0; index9 < count9; index9++)
            {
                const element9:uint = vector9 + 4 + index9 * 1;
                destination.bv[index9] = li8(element9);
            }

            const field10:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 24, 4);
            const vector10:uint = as3flatbuffers.Unpack.vector(context, field10, 2);
            const count10:uint = vector10 ? uint(li32(vector10)) : 0;
            destination.cv.length = count10;

            for (var index10:uint = 0; index10 < count10; index10++)
            {
                const element10:uint = vector10 + 4 + index10 * 2;
                destination.cv[index10] = sxi16(li16(element10));
            }

            const field11:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 26, 4);
            const vector11:uint = as3flatbuffers.Unpack.vector(context, field11, 2);
            const count11:uint = vector11 ? uint(li32(vector11)) : 0;
            destination.dv.length = count11;

            for (var index11:uint = 0; index11 < count11; index11++)
            {
                const element11:uint = vector11 + 4 + index11 * 2;
                destination.dv[index11] = li16(element11);
            }

            const field12:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 28, 4);
            const vector12:uint = as3flatbuffers.Unpack.vector(context, field12, 4);
            const count12:uint = vector12 ? uint(li32(vector12)) : 0;
            destination.ev.length = count12;

            for (var index12:uint = 0; index12 < count12; index12++)
            {
                const element12:uint = vector12 + 4 + index12 * 4;
                destination.ev[index12] = li32(element12);
            }

            const field13:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 30, 4);
            const vector13:uint = as3flatbuffers.Unpack.vector(context, field13, 4);
            const count13:uint = vector13 ? uint(li32(vector13)) : 0;
            destination.fv.length = count13;

            for (var index13:uint = 0; index13 < count13; index13++)
            {
                const element13:uint = vector13 + 4 + index13 * 4;
                destination.fv[index13] = uint(li32(element13));
            }

            const field14:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 32, 4);
            const vector14:uint = as3flatbuffers.Unpack.vector(context, field14, 8);
            const count14:uint = vector14 ? uint(li32(vector14)) : 0;
            destination.gv.length = count14;

            for (var index14:uint = 0; index14 < count14; index14++)
            {
                const element14:uint = vector14 + 4 + index14 * 8;
                if (!destination.gv[index14])
                    destination.gv[index14] = new as3flatbuffers.types.Int64();
                destination.gv[index14].set(uint(li32(element14)), li32(element14 + 4));
            }

            const field15:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 34, 4);
            const vector15:uint = as3flatbuffers.Unpack.vector(context, field15, 8);
            const count15:uint = vector15 ? uint(li32(vector15)) : 0;
            destination.hv.length = count15;

            for (var index15:uint = 0; index15 < count15; index15++)
            {
                const element15:uint = vector15 + 4 + index15 * 8;
                if (!destination.hv[index15])
                    destination.hv[index15] = new as3flatbuffers.types.UInt64();
                destination.hv[index15].set(uint(li32(element15)), uint(li32(element15 + 4)));
            }

            const position16:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 36, 1);
            if (!position16)
            {
                destination.optionalMode = null;
            }
            else
            {
                if (!destination.optionalMode)
                    destination.optionalMode = new as3flatbuffers.types.OptionalUint();

                destination.optionalMode.value = li8(position16);
            }

            const position17:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 38, 8);
            if (!position17)
            {
                destination.optionalWide = null;
            }
            else
            {
                if (!destination.optionalWide)
                    destination.optionalWide = new as3flatbuffers.types.UInt64();

                destination.optionalWide.set(uint(li32(position17)), uint(li32(position17 + 4)));
            }

            const position18:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 40, 32);
            destination.state = position18 ? fixtures.enums.EnumStruct.unpackFrom(context, position18, destination.state) : null;

            const position19:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 42, 4);
            if (!position19)
            {
                destination.flags = 0;
            }
            else
            {
                destination.flags = uint(li32(position19));
            }
            return destination;
        }
    }
}
