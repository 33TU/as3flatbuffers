// Code generated by as3flatc. DO NOT EDIT.
package fixtures.enums
{
    import as3flatbuffers.TableView;
    import flash.utils.ByteArray;
    import as3flatbuffers.types.Int64;
    import as3flatbuffers.types.UInt64;
    import as3flatbuffers.types.OptionalUint;
    import fixtures.enums.EnumStruct;
    import fixtures.enums.EnumStructView;

    /** Borrowed read-only view; unpack() produces independent owned values. */
    public final class EnumsView extends as3flatbuffers.TableView
    {
        private const stateView:fixtures.enums.EnumStructView = new fixtures.enums.EnumStructView();

        public function get a():int
        {
            const position:uint = fieldOffset(4, 1);
            if (!position)
                return -128;

            bytes.position = position;
            return bytes.readByte();
        }

        public function get b():uint
        {
            const position:uint = fieldOffset(6, 1);
            if (!position)
                return 0;

            bytes.position = position;
            return bytes.readUnsignedByte();
        }

        public function get c():int
        {
            const position:uint = fieldOffset(8, 2);
            if (!position)
                return -32768;

            bytes.position = position;
            return bytes.readShort();
        }

        public function get d():uint
        {
            const position:uint = fieldOffset(10, 2);
            if (!position)
                return 0;

            bytes.position = position;
            return bytes.readUnsignedShort();
        }

        public function get e():int
        {
            const position:uint = fieldOffset(12, 4);
            if (!position)
                return -2147483648;

            bytes.position = position;
            return bytes.readInt();
        }

        public function get f():uint
        {
            const position:uint = fieldOffset(14, 4);
            if (!position)
                return 0;

            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function get g():as3flatbuffers.types.Int64
        {
            const position:uint = fieldOffset(16, 8);
            if (!position)
                return new as3flatbuffers.types.Int64(1, 2097152);

            bytes.position = position;
            return new as3flatbuffers.types.Int64(bytes.readUnsignedInt(), bytes.readInt());
        }

        public function get h():as3flatbuffers.types.UInt64
        {
            const position:uint = fieldOffset(18, 8);
            if (!position)
                return new as3flatbuffers.types.UInt64(0, 0);

            bytes.position = position;
            return new as3flatbuffers.types.UInt64(bytes.readUnsignedInt(), bytes.readUnsignedInt());
        }

        public function get avLength():uint
        {
            const position:uint = vectorOffset(20, 1);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function av(index:uint):int
        {
            const vector:uint = vectorOffset(20, 1);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 1;
            bytes.position = position;
            return bytes.readByte();
        }

        public function get bvLength():uint
        {
            const position:uint = vectorOffset(22, 1);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function bv(index:uint):uint
        {
            const vector:uint = vectorOffset(22, 1);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 1;
            bytes.position = position;
            return bytes.readUnsignedByte();
        }

        public function get cvLength():uint
        {
            const position:uint = vectorOffset(24, 2);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function cv(index:uint):int
        {
            const vector:uint = vectorOffset(24, 2);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 2;
            bytes.position = position;
            return bytes.readShort();
        }

        public function get dvLength():uint
        {
            const position:uint = vectorOffset(26, 2);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function dv(index:uint):uint
        {
            const vector:uint = vectorOffset(26, 2);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 2;
            bytes.position = position;
            return bytes.readUnsignedShort();
        }

        public function get evLength():uint
        {
            const position:uint = vectorOffset(28, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function ev(index:uint):int
        {
            const vector:uint = vectorOffset(28, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            bytes.position = position;
            return bytes.readInt();
        }

        public function get fvLength():uint
        {
            const position:uint = vectorOffset(30, 4);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function fv(index:uint):uint
        {
            const vector:uint = vectorOffset(30, 4);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 4;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function get gvLength():uint
        {
            const position:uint = vectorOffset(32, 8);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function gv(index:uint):as3flatbuffers.types.Int64
        {
            const vector:uint = vectorOffset(32, 8);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 8;
            bytes.position = position;
            return new as3flatbuffers.types.Int64(bytes.readUnsignedInt(), bytes.readInt());
        }

        public function get hvLength():uint
        {
            const position:uint = vectorOffset(34, 8);
            if (!position)
                return 0;
            bytes.position = position;
            return bytes.readUnsignedInt();
        }

        public function hv(index:uint):as3flatbuffers.types.UInt64
        {
            const vector:uint = vectorOffset(34, 8);
            if (!vector)
                throw new RangeError("Vector index is out of range");
            bytes.position = vector;
            if (index >= bytes.readUnsignedInt())
                throw new RangeError("Vector index is out of range");
            const position:uint = vector + 4 + index * 8;
            bytes.position = position;
            return new as3flatbuffers.types.UInt64(bytes.readUnsignedInt(), bytes.readUnsignedInt());
        }

        public function get optionalMode():as3flatbuffers.types.OptionalUint
        {
            const position:uint = fieldOffset(36, 1);
            if (!position)
                return null;

            bytes.position = position;
            return new as3flatbuffers.types.OptionalUint(bytes.readUnsignedByte());
        }

        public function get optionalWide():as3flatbuffers.types.UInt64
        {
            const position:uint = fieldOffset(38, 8);
            if (!position)
                return null;

            bytes.position = position;
            return new as3flatbuffers.types.UInt64(bytes.readUnsignedInt(), bytes.readUnsignedInt());
        }

        public function get state():fixtures.enums.EnumStructView
        {
            const position:uint = fieldOffset(40, 32);
            return position ? this.stateView.bind(bytes, position) : null;
        }

        public function get flags():uint
        {
            const position:uint = fieldOffset(42, 4);
            if (!position)
                return 0;

            bytes.position = position;
            return bytes.readUnsignedInt();
        }
    }
}
