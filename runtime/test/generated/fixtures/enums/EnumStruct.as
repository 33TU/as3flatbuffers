// Code generated by as3flatc. DO NOT EDIT.
package fixtures.enums
{
    import as3flatbuffers.Pack;
    import as3flatbuffers.Unpack;
    import as3flatbuffers.UnpackContext;
    import avm2.intrinsics.memory.*;
    import flash.utils.ByteArray;
    import as3flatbuffers.types.Int64;
    import as3flatbuffers.types.UInt64;

    /** Owned inline struct. pack() writes raw struct bytes into the destination. */
    public final class EnumStruct
    {
        private static const PACK:as3flatbuffers.PackContext = new as3flatbuffers.PackContext();
        private static const UNPACK:as3flatbuffers.UnpackContext = new as3flatbuffers.UnpackContext();

        public var a:int = 0;
        public var b:uint = 0;
        public var c:int = 0;
        public var d:uint = 0;
        public var e:int = 0;
        public var f:uint = 0;
        public var g:as3flatbuffers.types.Int64 = new as3flatbuffers.types.Int64(0, 0);
        public var h:as3flatbuffers.types.UInt64 = new as3flatbuffers.types.UInt64(0, 0);

        public static function reset(msg:EnumStruct):void
        {
            msg.a = 0;
            msg.b = 0;
            msg.c = 0;
            msg.d = 0;
            msg.e = 0;
            msg.f = 0;
            msg.g.reset();
            msg.h.reset();
        }

        public static function clone(source:EnumStruct):EnumStruct
        {
            if (!source)
                return null;

            const destination:EnumStruct = new EnumStruct();
            destination.a = source.a;
            destination.b = source.b;
            destination.c = source.c;
            destination.d = source.d;
            destination.e = source.e;
            destination.f = source.f;
            destination.g.copyFrom(source.g);
            destination.h.copyFrom(source.h);

            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function pack(source:EnumStruct, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.begin(context, dst, false);
                as3flatbuffers.Pack.finish(context, packInto(source, context));
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Write into an active context and return the absolute object offset. */
        public static function packInto(source:EnumStruct, context:as3flatbuffers.PackContext):uint
        {
            if (!source || !context)
                throw new ArgumentError("Source and context must be non-null");

            const start:uint = as3flatbuffers.Pack.prepareStruct(context, 32, 8);
            si8(source.a, start + 0);
            si8(source.b, start + 1);
            si16(source.c, start + 2);
            si16(source.d, start + 4);
            si16(0, start + 6);
            si32(source.e, start + 8);
            si32(source.f, start + 12);
            if (!source.g)
                throw new ArgumentError("source.g must be non-null");
            si32(source.g.low, start + 16);
            si32(source.g.high, start + 16 + 4);
            if (!source.h)
                throw new ArgumentError("source.h must be non-null");
            si32(source.h.low, start + 24);
            si32(source.h.high, start + 24 + 4);
            return start;
        }

        /** Decode raw struct bytes at offset into a new or reused owned value. */
        public static function unpack(bytes:flash.utils.ByteArray, destination:EnumStruct = null, offset:uint = 0):EnumStruct
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.begin(context, bytes);
            try
            {
                destination = unpackFrom(context, offset, destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }

        /** Internal generated entry point; context must already own domain memory. */
        public static function unpackFrom(context:as3flatbuffers.UnpackContext, base:uint, destination:EnumStruct):EnumStruct
        {
            as3flatbuffers.Unpack.struct(context, base, 32);
            if (!destination)
                destination = new EnumStruct();

            destination.a = sxi8(li8(base + 0));

            destination.b = li8(base + 1);

            destination.c = sxi16(li16(base + 2));

            destination.d = li16(base + 4);

            destination.e = li32(base + 8);

            destination.f = uint(li32(base + 12));

            if (!destination.g)
                destination.g = new as3flatbuffers.types.Int64();

            destination.g.set(uint(li32(base + 16)), li32(base + 20));

            if (!destination.h)
                destination.h = new as3flatbuffers.types.UInt64();

            destination.h.set(uint(li32(base + 24)), uint(li32(base + 28)));
            return destination;
        }
    }
}
