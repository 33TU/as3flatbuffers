// Code generated by as3flatc. DO NOT EDIT.
package bench.data
{
    import as3flatbuffers.Pack;
    import as3flatbuffers.Unpack;
    import as3flatbuffers.UnpackContext;
    import avm2.intrinsics.memory.*;
    import flash.utils.ByteArray;
    import bench.data.VectorItem;

    /** Owned mutable value. pack() writes a complete FlatBuffer into the destination. */
    public final class VectorTables
    {
        private static const PACK:as3flatbuffers.PackContext = new as3flatbuffers.PackContext();
        private static const UNPACK:as3flatbuffers.UnpackContext = new as3flatbuffers.UnpackContext();

        public var sequence:uint = 0;
        public var points:Vector.<bench.data.VectorItem> = new Vector.<bench.data.VectorItem>();

        public static function reset(msg:VectorTables):void
        {
            msg.sequence = 0;
            msg.points.length = 0;
        }

        public static function clone(source:VectorTables):VectorTables
        {
            if (!source)
                return null;

            const destination:VectorTables = new VectorTables();
            destination.sequence = source.sequence;
            destination.points.length = source.points.length;
            for (var index1:uint = 0; index1 < source.points.length; index1++)
            {
                destination.points[index1] = bench.data.VectorItem.clone(source.points[index1]);
            }

            return destination;
        }

        /** Replace dst with packed bytes. Caller must select little-endian. Returns dst at position zero. */
        public static function pack(source:VectorTables, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.begin(context, dst, true);
                as3flatbuffers.Pack.finish(context, packInto(source, context));
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Write into an active context and return the absolute object offset. */
        public static function packInto(source:VectorTables, context:as3flatbuffers.PackContext):uint
        {
            if (!source || !context)
                throw new ArgumentError("Source and context must be non-null");

            as3flatbuffers.Pack.prepare(context, 2);
            as3flatbuffers.Pack.reserveVtable(context, 2);
            as3flatbuffers.Pack.prepare(context, 4);
            as3flatbuffers.Pack.startTable(context);
            if (source.sequence != 0)
            {
                as3flatbuffers.Pack.addUint32(context, 0, source.sequence);
            }
            const offset1:uint = source.points.length ? as3flatbuffers.Pack.reserveOffset(context, 1) : 0;
            const table:uint = as3flatbuffers.Pack.endTable(context);
            if (offset1)
            {
                const bytes1:flash.utils.ByteArray = as3flatbuffers.Pack.prepareVector(context, 4);
                const vector1:uint = as3flatbuffers.Pack.startVector(context, source.points.length);
                as3flatbuffers.Pack.patchOffset(context, offset1, vector1);
                const data1:uint = bytes1.position;
                for (var reserve1:uint = 0; reserve1 < source.points.length; reserve1++)
                    bytes1.writeUnsignedInt(0);
                for (var index1:uint = 0; index1 < source.points.length; index1++)
                {
                    if (source.points[index1] == null)
                        throw new ArgumentError("points elements must be non-null");
                    const child1:uint = bench.data.VectorItem.packInto(source.points[index1], context);
                    as3flatbuffers.Pack.patchOffset(context, data1 + index1 * 4, child1);
                }
            }
            return table;
        }

        /** Decode a FlatBuffer whose root-offset word starts at offset. */
        public static function unpack(bytes:flash.utils.ByteArray, destination:VectorTables = null, offset:uint = 0):VectorTables
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.begin(context, bytes);
            try
            {
                destination = unpackFrom(context, as3flatbuffers.Unpack.root(context, offset), destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }

        /** Internal generated entry point; context must already own domain memory. */
        public static function unpackFrom(context:as3flatbuffers.UnpackContext, base:uint, destination:VectorTables):VectorTables
        {
            const vtable:uint = as3flatbuffers.Unpack.vtable(context, base);
            const vtableSize:uint = li16(vtable);
            const objectSize:uint = li16(vtable + 2);
            if (!destination)
                destination = new VectorTables();

            const position0:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 4, 4);
            if (!position0)
            {
                destination.sequence = 0;
            }
            else
            {
                destination.sequence = uint(li32(position0));
            }

            const field1:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 6, 4);
            const vector1:uint = as3flatbuffers.Unpack.vector(context, field1, 4);
            const count1:uint = vector1 ? uint(li32(vector1)) : 0;
            destination.points.length = count1;

            for (var index1:uint = 0; index1 < count1; index1++)
            {
                const element1:uint = vector1 + 4 + index1 * 4;
                const child1:uint = as3flatbuffers.Unpack.tableOffset(context, element1);
                destination.points[index1] = bench.data.VectorItem.unpackFrom(context, child1, destination.points[index1]);
            }
            return destination;
        }
    }
}
