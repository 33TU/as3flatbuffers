// Code generated by as3flatc. DO NOT EDIT.
package bench.data
{
    import as3flatbuffers.Builder;
    import flash.utils.ByteArray;

    /** Owned mutable value. pack() writes a complete FlatBuffer into the destination. */
    public final class StringMessage
    {
        private static const BUILDER:as3flatbuffers.Builder = new as3flatbuffers.Builder();

        public var sequence:uint = 0;
        public var name:String = null;
        public var text:String = null;
        public var details:String = null;

        public static function reset(msg:StringMessage):void
        {
            msg.sequence = 0;
            msg.name = null;
            msg.text = null;
            msg.details = null;
        }

        public static function clone(source:StringMessage):StringMessage
        {
            if (!source)
                return null;

            const destination:StringMessage = new StringMessage();
            destination.sequence = source.sequence;
            destination.name = source.name;
            destination.text = source.text;
            destination.details = source.details;

            return destination;
        }

        /** Replace dst with packed bytes. Caller must select little-endian. Returns dst at position zero. */
        public static function pack(source:StringMessage, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const builder:as3flatbuffers.Builder = BUILDER;
            try
            {
                builder.reset(dst, true);
                builder.finish(packInto(source, builder));
            }
            finally
            {
                builder.reset();
            }
            return dst;
        }

        /** Write into an active builder and return the absolute object offset. */
        public static function packInto(source:StringMessage, builder:as3flatbuffers.Builder):uint
        {
            if (!source || !builder)
                throw new ArgumentError("Source and builder must be non-null");

            builder.startTable(4, 4);
            if (source.sequence != 0)
                builder.addUint32(0, source.sequence);
            const offset1:uint = source.name != null ? builder.reserveOffset(1) : 0;
            const offset2:uint = source.text != null ? builder.reserveOffset(2) : 0;
            const offset3:uint = source.details != null ? builder.reserveOffset(3) : 0;
            const table:uint = builder.endTable();
            if (offset1)
                builder.patchOffset(offset1, builder.createString(source.name));
            if (offset2)
                builder.patchOffset(offset2, builder.createString(source.text));
            if (offset3)
                builder.patchOffset(offset3, builder.createString(source.details));
            return table;
        }
    }
}
