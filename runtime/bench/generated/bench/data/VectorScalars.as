// Code generated by as3flatc. DO NOT EDIT.
package bench.data
{
    import as3flatbuffers.Pack;
    import as3flatbuffers.Unpack;
    import as3flatbuffers.UnpackContext;
    import avm2.intrinsics.memory.*;
    import flash.utils.ByteArray;

    /** Owned mutable value. pack() writes a complete FlatBuffer into the destination. */
    public final class VectorScalars
    {
        private static const PACK:as3flatbuffers.PackContext = new as3flatbuffers.PackContext();
        private static const UNPACK:as3flatbuffers.UnpackContext = new as3flatbuffers.UnpackContext();

        public var sequence:uint = 0;
        public var deltas:Vector.<int> = new Vector.<int>();
        public var checksums:Vector.<uint> = new Vector.<uint>();
        public var weights:Vector.<Number> = new Vector.<Number>();

        public static function reset(msg:VectorScalars):void
        {
            msg.sequence = 0;
            msg.deltas.length = 0;
            msg.checksums.length = 0;
            msg.weights.length = 0;
        }

        public static function clone(source:VectorScalars):VectorScalars
        {
            if (!source)
                return null;

            const destination:VectorScalars = new VectorScalars();
            destination.sequence = source.sequence;
            destination.deltas = source.deltas.concat();
            destination.checksums = source.checksums.concat();
            destination.weights = source.weights.concat();

            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function pack(source:VectorScalars, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.begin(context, dst, true);
                as3flatbuffers.Pack.finish(context, packInto(source, context));
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Write into an active context and return the absolute object offset. */
        public static function packInto(source:VectorScalars, context:as3flatbuffers.PackContext):uint
        {
            if (!source || !context)
                throw new ArgumentError("Source and context must be non-null");

            as3flatbuffers.Pack.ensure(context, 48);
            as3flatbuffers.Pack.prepare(context, 2);
            as3flatbuffers.Pack.reserveVtable(context, 4);
            as3flatbuffers.Pack.prepare(context, 4);
            as3flatbuffers.Pack.startTable(context);
            if (source.sequence != 0)
            {
                as3flatbuffers.Pack.addUint32(context, 0, source.sequence);
            }
            const offset1:uint = source.deltas.length ? as3flatbuffers.Pack.reserveOffset(context, 1) : 0;
            const offset2:uint = source.checksums.length ? as3flatbuffers.Pack.reserveOffset(context, 2) : 0;
            const offset3:uint = source.weights.length ? as3flatbuffers.Pack.reserveOffset(context, 3) : 0;
            const table:uint = as3flatbuffers.Pack.endTable(context);
            if (offset1)
            {
                as3flatbuffers.Pack.prepareVector(context, 4, source.deltas.length, 4);
                const vector1:uint = as3flatbuffers.Pack.startVector(context, source.deltas.length);
                as3flatbuffers.Pack.patchOffset(context, offset1, vector1);
                const data1:uint = as3flatbuffers.Pack.reserve(context, source.deltas.length * 4);
                for (var index1:uint = 0; index1 < source.deltas.length; index1++)
                {
                    si32(source.deltas[index1], data1 + index1 * 4);
                }
            }
            if (offset2)
            {
                as3flatbuffers.Pack.prepareVector(context, 4, source.checksums.length, 4);
                const vector2:uint = as3flatbuffers.Pack.startVector(context, source.checksums.length);
                as3flatbuffers.Pack.patchOffset(context, offset2, vector2);
                const data2:uint = as3flatbuffers.Pack.reserve(context, source.checksums.length * 4);
                for (var index2:uint = 0; index2 < source.checksums.length; index2++)
                {
                    si32(source.checksums[index2], data2 + index2 * 4);
                }
            }
            if (offset3)
            {
                as3flatbuffers.Pack.prepareVector(context, 4, source.weights.length, 4);
                const vector3:uint = as3flatbuffers.Pack.startVector(context, source.weights.length);
                as3flatbuffers.Pack.patchOffset(context, offset3, vector3);
                const data3:uint = as3flatbuffers.Pack.reserve(context, source.weights.length * 4);
                for (var index3:uint = 0; index3 < source.weights.length; index3++)
                {
                    sf32(source.weights[index3], data3 + index3 * 4);
                }
            }
            return table;
        }

        /** Decode a FlatBuffer whose root-offset word starts at offset. */
        public static function unpack(bytes:flash.utils.ByteArray, destination:VectorScalars = null, offset:uint = 0):VectorScalars
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.begin(context, bytes);
            try
            {
                destination = unpackFrom(context, as3flatbuffers.Unpack.root(context, offset), destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }

        /** Internal generated entry point; context must already own domain memory. */
        public static function unpackFrom(context:as3flatbuffers.UnpackContext, base:uint, destination:VectorScalars):VectorScalars
        {
            const vtable:uint = as3flatbuffers.Unpack.vtable(context, base);
            const vtableSize:uint = li16(vtable);
            const objectSize:uint = li16(vtable + 2);
            if (!destination)
                destination = new VectorScalars();

            const position0:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 4, 4);
            if (!position0)
            {
                destination.sequence = 0;
            }
            else
            {
                destination.sequence = uint(li32(position0));
            }

            const field1:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 6, 4);
            const vector1:uint = as3flatbuffers.Unpack.vector(context, field1, 4);
            const count1:uint = vector1 ? uint(li32(vector1)) : 0;
            destination.deltas.length = count1;

            for (var index1:uint = 0; index1 < count1; index1++)
            {
                const element1:uint = vector1 + 4 + index1 * 4;
                destination.deltas[index1] = li32(element1);
            }

            const field2:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 8, 4);
            const vector2:uint = as3flatbuffers.Unpack.vector(context, field2, 4);
            const count2:uint = vector2 ? uint(li32(vector2)) : 0;
            destination.checksums.length = count2;

            for (var index2:uint = 0; index2 < count2; index2++)
            {
                const element2:uint = vector2 + 4 + index2 * 4;
                destination.checksums[index2] = uint(li32(element2));
            }

            const field3:uint = as3flatbuffers.Unpack.fieldOffset(vtable, vtableSize, objectSize, base, 10, 4);
            const vector3:uint = as3flatbuffers.Unpack.vector(context, field3, 4);
            const count3:uint = vector3 ? uint(li32(vector3)) : 0;
            destination.weights.length = count3;

            for (var index3:uint = 0; index3 < count3; index3++)
            {
                const element3:uint = vector3 + 4 + index3 * 4;
                destination.weights[index3] = lf32(element3);
            }
            return destination;
        }
    }
}
