// Code generated by as3flatc. DO NOT EDIT.
package bench.data
{
    import as3flatbuffers.Pack;
    import as3flatbuffers.Unpack;
    import as3flatbuffers.UnpackContext;
    import avm2.intrinsics.memory.*;
    import flash.utils.ByteArray;
    import bench.data.Vec3;

    /** Owned inline struct. pack() writes raw struct bytes into the destination. */
    public final class State
    {
        private static const PACK:as3flatbuffers.PackContext = new as3flatbuffers.PackContext();
        private static const UNPACK:as3flatbuffers.UnpackContext = new as3flatbuffers.UnpackContext();

        public var position:bench.data.Vec3 = new bench.data.Vec3();
        public var velocity:bench.data.Vec3 = new bench.data.Vec3();
        public var facing:bench.data.Vec3 = new bench.data.Vec3();
        public var delta:int = 0;
        public var checksum:uint = 0;
        public var precision:Number = 0;
        public var active:Boolean = false;
        public var kind:uint = 0;

        public static function reset(msg:State):void
        {
            bench.data.Vec3.reset(msg.position);
            bench.data.Vec3.reset(msg.velocity);
            bench.data.Vec3.reset(msg.facing);
            msg.delta = 0;
            msg.checksum = 0;
            msg.precision = 0;
            msg.active = false;
            msg.kind = 0;
        }

        public static function clone(source:State):State
        {
            if (!source)
                return null;

            const destination:State = new State();
            destination.position = bench.data.Vec3.clone(source.position);
            destination.velocity = bench.data.Vec3.clone(source.velocity);
            destination.facing = bench.data.Vec3.clone(source.facing);
            destination.delta = source.delta;
            destination.checksum = source.checksum;
            destination.precision = source.precision;
            destination.active = source.active;
            destination.kind = source.kind;

            return destination;
        }

        /** Replace dst with packed bytes. Writes little-endian without changing dst.endian. Returns dst at position zero. */
        public static function pack(source:State, dst:flash.utils.ByteArray):flash.utils.ByteArray
        {
            if (!source || !dst)
                throw new ArgumentError("Source and destination must be non-null");

            const context:as3flatbuffers.PackContext = PACK;
            try
            {
                as3flatbuffers.Pack.begin(context, dst, false);
                as3flatbuffers.Pack.finish(context, packInto(source, context));
            }
            finally
            {
                as3flatbuffers.Pack.reset(context);
            }
            return dst;
        }

        /** Write into an active context and return the absolute object offset. */
        public static function packInto(source:State, context:as3flatbuffers.PackContext):uint
        {
            if (!source || !context)
                throw new ArgumentError("Source and context must be non-null");

            const start:uint = as3flatbuffers.Pack.prepareStruct(context, 64, 8);
            if (!source.position)
                throw new ArgumentError("source.position must be non-null");
            sf32(source.position.x, start + 0);
            sf32(source.position.y, start + 4);
            sf32(source.position.z, start + 8);
            if (!source.velocity)
                throw new ArgumentError("source.velocity must be non-null");
            sf32(source.velocity.x, start + 12);
            sf32(source.velocity.y, start + 16);
            sf32(source.velocity.z, start + 20);
            if (!source.facing)
                throw new ArgumentError("source.facing must be non-null");
            sf32(source.facing.x, start + 24);
            sf32(source.facing.y, start + 28);
            sf32(source.facing.z, start + 32);
            si32(source.delta, start + 36);
            si32(source.checksum, start + 40);
            si32(0, start + 44);
            sf64(source.precision, start + 48);
            si8((source.active ? 1 : 0), start + 56);
            si16(0, start + 57);
            si8(0, start + 59);
            si32(source.kind, start + 60);
            return start;
        }

        /** Decode raw struct bytes at offset into a new or reused owned value. */
        public static function unpack(bytes:flash.utils.ByteArray, destination:State = null, offset:uint = 0):State
        {
            const context:as3flatbuffers.UnpackContext = UNPACK;
            const previous:flash.utils.ByteArray = as3flatbuffers.Unpack.begin(context, bytes);
            try
            {
                destination = unpackFrom(context, offset, destination);
            }
            finally
            {
                as3flatbuffers.Unpack.end(context, previous);
            }
            return destination;
        }

        /** Internal generated entry point; context must already own domain memory. */
        public static function unpackFrom(context:as3flatbuffers.UnpackContext, base:uint, destination:State):State
        {
            as3flatbuffers.Unpack.struct(context, base, 64);
            if (!destination)
                destination = new State();

            destination.position = bench.data.Vec3.unpackFrom(context, base + 0, destination.position);

            destination.velocity = bench.data.Vec3.unpackFrom(context, base + 12, destination.velocity);

            destination.facing = bench.data.Vec3.unpackFrom(context, base + 24, destination.facing);

            destination.delta = li32(base + 36);

            destination.checksum = uint(li32(base + 40));

            destination.precision = lf64(base + 48);

            destination.active = (li8(base + 56) != 0);

            destination.kind = uint(li32(base + 60));
            return destination;
        }
    }
}
